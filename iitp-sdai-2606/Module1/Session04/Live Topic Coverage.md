| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Create and manipulate lists using essential methods | covered | List creation taught (empty list, homogeneous and mixed data types, ordered/mutable nature); `append()` demonstrated to add elements at end; `pop()` and `pop(index)` taught for removing last or specific index; `sort()` demonstrated including descending order with `reverse=True`; `len()` used on lists after deletions and in indexing examples; live append-in-loop pattern used in end-of-class student marks exercise. |
| Access elements via indexing and slicing on lists and strings | covered | Positive indexing on lists explained (0-based start, last index = len − 1); negative indexing mentioned briefly but de-emphasised; list slicing taught in depth (start, stop, step, exclusive stop, sublist creation, full copy); string indexing demonstrated on `"PYTHON"` character-by-character; string slicing stated to follow same rules as lists with brief mention, but most slicing live demos were on lists; `IndexError` / index out of range explained with examples. |
| Format and concatenate strings using f-strings | not covered | String concatenation with `+` taught (`first_name + last_name`); type mismatch error shown when concatenating string with integer; `str()` conversion used to join text with numeric age; **f-strings were not introduced or demonstrated** in the live session. |
| Apply built-in functions to process list and string data | covered | `len()` applied to lists and strings; `max()`, `min()`, and `sum()` demonstrated on a collected `student_marks` list with average via `sum(...) / len(...)`; `min()` also briefly shown on a string (alphabetical/ASCII discussion); list ordering taught via **`.sort()` method** rather than the built-in `sorted()` function — `sorted()` as a non-mutating built-in was not explicitly demonstrated. |
| Extra concept taught: for loop over lists | covered | Instructor recapped iterating a list with `for i in range(len(scores))` and accessing `scores[i]`; linked for loops to list traversal as a major use case. |
| Extra concept taught: list reversal using slicing | covered | Demonstrated reversing a list with `[::-1]` slice notation; distinguished reversal from sorting. |
| Extra concept taught: live student marks collection program | covered | End-of-session coding exercise — take number of students, read name/marks/age, `append` into parallel lists, then print highest, lowest, and average using `max`, `min`, `sum`, and `len`. |
| Extra concept taught: brief recap of loops (for/while, break, continue) | covered | Short opening recap from previous session on when to use for vs while, break, and continue — not part of planned subtopics but time spent at session start. |
