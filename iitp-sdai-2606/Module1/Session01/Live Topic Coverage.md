| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Execute Python programs in an online compiler | covered | Compiler role explained (high-level code → binary); onlinecompiler.com/python demonstrated with code editor, Run button, and console output; Hello World program executed live; students guided to search for online Python compilers; Programiz recommended as an alternative for the first week. |
| Declare variables and work with core data types | covered | Variables introduced as labels holding values; declaration syntax demonstrated (`age = 10`, `name = "Yash"`); core data types covered — integer, string, float (decimal/double), and boolean; Python’s dynamic typing (no explicit type declaration) explained; string quoting syntax (single/double quotes) discussed. |
| Apply arithmetic, comparison, logical, and assignment operators | covered | Arithmetic operators (+, −, ×, /, modulus, power) with variable examples; comparison operators (==, !=, >, <, >=, <=) with true/false outcomes; logical operators (and, or, not) for combining conditions; assignment operator (=) for storing values in variables; operator expressions demonstrated (e.g., V3 = V1 + V2). |
| Handle basic user input and output using input() and print() | covered | `print()` taught from first program onward (Hello World, multiple print statements, formatted output); `input()` demonstrated with a live two-value sum program (prompt for V1 and V2, dynamic output based on user input); `int()` type conversion around `input()` explained because input returns string by default. |
| Extra concept taught: Introduction to programming and why Python | covered | What programming is (instructions to a computer); real-world app examples (Zomato, PhonePe, etc.); why Python is beginner-friendly vs languages with heavier syntax (curly brackets, semicolons). |
| Extra concept taught: Comments in Python | covered | Single-line comments with `#`; multi-line comments demonstrated; distinction between commented code and executable code explained. |
| Extra concept taught: Python functions (introductory) | covered | `print()` introduced as a built-in function; functions mentioned in session agenda with deeper coverage deferred. |
| Extra concept taught: End-of-session Mentimeter quiz | covered | Quiz reinforced variable syntax, comparison vs assignment operators (`==` vs `=`), and purpose of `input()` for taking user input. |
