| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Explain what a large language model is and how token-based text generation differs from rule-based software. | covered | Defined LLM as an ML model with billions of learned parameters trained on vast text to predict/generate human language (junior-copywriter and library-reading analogies); core behavior framed as text-in/text-out. Contrasted rule-based software (frozen flowcharts, IVR gas/airline booking, same input → same output) with token-based LLMs (prompt → tokens → one-token-at-a-time next-token prediction, autocomplete analogy). Extended comparison via rule-based chatbots (WhatsApp banking keyword matching) vs LLMs (handles novel phrases, probabilistic/non-deterministic answers). |
| Define tokens and context windows and predict when a prompt will exceed usable context. | covered | Tokens defined as smallest text units (brick/alphabet analogies; can be whole words, subwords, punctuation); ~0.75 words per token rule of thumb; Indian-language/mixed text increases token count. Live `tiktoken` demo counted tokens for English and Hinglish samples. Context window defined as max input+output tokens per API call (working memory); includes prompt, uploaded files, chat history, and system messages. Compared model limits (e.g., ChatGPT ~750 pages, Gemini ~800+ pages). Live Python overflow check demo with model limit, reply reserve, and safety buffer to predict when a long prompt exceeds usable context. |
| Describe hallucinations and other common failure modes in plain language with examples. | not covered | Hallucinations were flagged in the session agenda and briefly mentioned (probability-based generation, junior-copywriter “confident facts” analogy, ChatGPT disclaimer vs mutual-fund risk warning, RAG preview as a future fix). No dedicated segment with plain-language definitions, multiple concrete examples, or coverage of other common failure modes. Transcript ends (~1:46) during context-window discussion before this topic was taught in depth. |
| Relate GenAI capabilities to prior ML ideas without requiring neural-network mathematics. | covered | Recapped prior predictive ML (regression, classification, unsupervised, time series) and contrasted predictive vs generative models (house-price query scoped to training data vs open-ended generation). Mapped classical ML pipeline (labeled rows, numeric features, one task-specific model, RMSE-style evaluation) to GenAI (massive multimodal text, tokens as features, one general model for many language tasks, usefulness/safety/grounding checks). Compared random forest on small tabular data to LLMs trained on Wikipedia/Project Gutenberg-scale text; no neural-network math required. |
| Extra: Rise of LLMs / historical arc | covered | Brief history from binary/text encoding → RNNs (left-to-right, limited context) → “Attention Is All You Need” / transformers (context example: trophy/bag sentence) → pre-training at scale and ChatGPT-era adoption in academia and industry; Word2Vec and compute bottlenecks mentioned. |
| Extra: When to use rule-based systems vs LLMs | covered | Rule-based preferred for predictable deterministic tasks (OTP authentication); LLMs better for summaries/drafts; warned against using LLMs for everything or as a lookup database. |
| Extra: Grammar correction vs strict rules (student Q&A) | covered | Explained grammatically fluent output as probabilistic pattern learning from well-formed training text, not rule execution; noted Grammarly can still flag LLM-generated text. |
| Extra: LLM providers and specialized models | covered | Named OpenAI, Llama, Anthropic, Grok; discussed code-specialized variants (e.g., Codex) built by further training base models on code. |
| Extra: Practical context-window guidance | covered | Advised against uploading very large PDFs in one shot; split documents and ask targeted questions; noted retrieval often matters more than simply enlarging prompts. |
| Extra: Preview of next module (prompting, RAG, tools) | covered | Framed today’s mental models as foundation for upcoming modules on prompting, RAG, and tool use. |
