| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Measure token counts for real prompts and relate them to billing; latency; and length trade-offs (build on S23 concepts at applied depth) | covered | Tokens defined (smallest text unit; subword tokenization with “unbelievable” example); input vs output (prompt vs completion) tokens explained; OpenAI pricing page walked through ($/million tokens); live Groq platform usage shown with real input/output token counts and latency comparison across requests; cost-benefit trade-off taught — richer prompts (system + user, few-shot, chain-of-thought from S24) increase input tokens, billing, and latency; `tiktoken` package described for programmatic estimation (apply to prior demos) though no live `tiktoken` code run — platforms track usage in practice; `max_tokens` introduced to cap output length and cost. |
| Explain how context window limits shape system prompt; few-shot example; and conversation history choices from S24 | covered | Context window defined as max tokens (input + output) per request; model comparisons (Gemini/Claude ~1M vs OpenAI 128K–256K; historical 4K–8K); linked back to S24 — more few-shot examples, detailed instructions, and chain-of-thought improve quality but consume prompt budget and raise cost/latency; system + user messages counted together as prompt tokens; long conversation history illustrated via WhatsApp export exercise (years of chat exceeding window); file attachments (Gemini) treated as part of user input eating context — design implication is balancing prompt richness, examples, and history against the window. |
| Configure temperature or seed (where available) to trade creativity vs consistency when testing prompt variants | covered | Temperature explained as controlling output variability (creativity vs consistency); live Groq API demo with marketing-copy use case — high temperature re-run multiple times yielding visibly different answers; low/zero temperature discussed for near-identical, consistent outputs; softmax intuition mentioned; ChatGPT default ~0.6–0.7 noted; instructor explicitly tried temperature 0 vs higher values on same prompt. **Seed parameter was not mentioned or demonstrated.** |
| Predict user-visible effects when context is truncated or overloaded — forgotten instructions; inconsistent tone; or sudden factual drift in long chats | covered | Context overflow / forced truncation taught with WhatsApp-transcript case study — model silently drops early/middle portions (no error banner); older chat segments may be missing while recent ones remain; unexpected or incomplete outputs when context is exceeded; “pages ripped from the middle of a book” analogy; instructor urged students to upload large transcripts to Gemini/ChatGPT to observe truncation firsthand. **Forgotten instructions, inconsistent tone, and factual drift were not named explicitly** — effects taught mainly as silent truncation, lost early context, and surprising/wrong outputs. |
| Extra concept taught: `max_tokens` / `max_completion_tokens` parameter | covered | Live Python demo restricting output size on Groq client; tied to billing and latency control. |
| Extra concept taught: Groq API platform — request IDs, usage logs, latency monitoring | covered | Dashboard walkthrough showing per-request input/output tokens and latency; enterprise observability angle discussed. |
| Extra concept taught: Subword tokenization internals | covered | Brief explanation that tokens ≠ words (e.g. “unbelievable” → 3 tokens); reusable subword units across contexts. |
| Extra concept taught: Zero-shot prompting recap in Python | covered | Earlier session demos ported to Groq API (`client.chat.completions.create`); system/user message structure recapped from S17/S24. |
| Extra concept taught: Softmax / next-token prediction intuition | covered | High-level link between temperature and probability distribution over next tokens; optional deep-dive on softmax for interested students. |
