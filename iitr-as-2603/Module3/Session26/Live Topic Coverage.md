| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Explain how client applications communicate with REST APIs using HTTP methods and status codes. | covered | REST defined (representational state transfer); request/response model taught with restaurant-waiter analogy; request components covered — URL/endpoint, HTTP method (GET for read, POST for write/update/delete), headers, params; response split into status code + body; HTTP status codes explained — 2xx success (200), 401 unauthorized (bad API key), 404 not found (wrong endpoint/model), 429 rate limit, 500 server error; linked to prior Groq `client.chat.completions.create` calls and Groq dashboard logs; real-world client examples via Gemini talking to Google Photos, Search, and Gmail through APIs. |
| Read and write JSON structures that represent requests and responses in Python. | covered | JSON defined as universal text format for structured data exchange; mapped to Python dict key–value structure; read paths demonstrated — `json.load` / `json.loads` on file and string, `response.json()` from HTTP response; write path demonstrated — `json.dumps` dict → JSON string; live Colab demo loading `demo.json` as a dict and extracting nested keys (e.g. `current` → `temperature_2m`); simulated order-tracking JSON string parsed to dict; noted JSON is the wire format behind LLM provider requests/responses. |
| Execute a GET request with the requests library and handle common response errors safely. | covered | Live `requests.get` demo against Open-Meteo weather API with URL, query `params` (city, latitude, longitude), and `timeout`; response inspected for status code (200 = success) before calling `response.json()`; common errors discussed (401, 404, 429, 500) with Groq log walkthrough and a simulated 404 via wrong model name; **formal `try`/`except` or `raise_for_status` patterns were not shown** — error handling taught as check-status-then-parse. |
| Map JSON fields from an API response into variables used in a downstream LLM prompt. | covered | End-to-end weather pipeline — GET weather JSON → parse dict → extract city, temperature, weather code, description into variables → build dynamic user prompt (“use only the live weather facts below”) → Groq API call for event-setup advice; same pattern repeated in extra demos — Fake Store e-commerce sales pitch, Frankfurter forex trade advisor, GitHub repo API; instructor stressed not feeding raw JSON to the LLM to save tokens. |
| Extra concept taught: Gemini as LLM + external APIs (personal context / tool use) | covered | Live demos of Gemini connecting to Google Photos, Search, and Gmail to answer questions a plain LLM cannot — frames APIs as the bridge between client and backend services. |
| Extra concept taught: API as contract / function-call mental model | covered | API defined as interface between two software components; request in, response out; tied to agent design for future sessions. |
| Extra concept taught: Free vs API-key-based services | covered | Open-Meteo, Fake Store, Frankfurter, and GitHub API shown without keys; contrasted with Groq and other paid/keyed services. |
| Extra concept taught: POST and CRUD HTTP verbs (brief) | covered | POST for insert/update/delete illustrated with orders example; session focus explicitly on GET only. |
| Extra concept taught: Groq full JSON response parsing | covered | Showed complete `completions` response object structure behind the simplified client wrapper. |
| Extra concept taught: Rate limits and token quotas (429) | covered | Groq free-tier limits (tokens/min, requests/min) explained with IRCTC/income-tax server-crash analogy; 429 errors shown in platform logs. |
| Extra concept taught: Poll / Q&A on grounding vs JSON syntax vs endpoint errors | covered | End-of-session MCQ discussion distinguishing wrong endpoint (non-200) from valid response with bad field names when status is 200. |
