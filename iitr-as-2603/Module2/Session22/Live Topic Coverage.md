| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Metric tables | covered | Built side-by-side comparison tables placing accuracy, precision, recall, and F1 score for multiple models (logistic regression, decision tree, random forest) on Wisconsin breast cancer data; emphasized F1 over raw accuracy when class imbalance matters; used tables to spot overfitting (train vs test gap). |
| Comparison by complexity | covered | Compared models from low to high complexity (logistic regression → decision tree → random forest); manual max_depth tuning demo showing underfitting vs overfitting; linked complexity to compute cost vs marginal accuracy gains; reinforced Occam's Razor / start-simple-then-add-complexity principle. |
| Model persistence (Saving/Loading) | covered | Demonstrated saving best pipeline with `joblib.dump` and loading with `joblib.load` on disk; "train once, load anytime" philosophy; cross-notebook / cross-machine inference demo; compared Joblib (preferred for sklearn) vs pickle; noted saving fitted preprocessing inside pipeline. |
| Selection checklist | covered | Summarized criteria: problem type (regression vs classification), data size, interpretability (decision tree vs random forest), speed, accuracy/R²/F1, linearity vs non-linearity, overfitting risk, and outlier/noise handling; advised documenting choices and starting with a simple baseline model. |
| Extra: Evaluation metrics deep-dive (accuracy, F1, precision, recall, confusion matrix) | covered | Substantial time on why accuracy alone can mislead (diabetes/cancer demo with 0% F1 for positive class despite ~78% accuracy); harmonic mean formula for F1; classification report usage. |
| Extra: sklearn Pipelines | covered | Bundled preprocessing (scaling, one-hot encoding) with models in pipelines for LR, DT, and RF on classification and regression workflows. |
| Extra: Hyperparameter tuning (manual + GridSearchCV) | covered | Manual decision-tree depth comparison; automated tuning with `GridSearchCV` on California housing random forest (`n_estimators`); selected best RF pipeline from grid search. |
| Extra: Gradio model deployment UI | covered | Built a Gradio web interface loading the persisted housing model for end-user predictions without re-importing sklearn; framed as client-facing inference demo. |
| Extra: Pickle security risks | covered | Discussed in mentimeter Q&A that loading untrusted Joblib/pickle files can execute malicious code; reinforced saving full fitted pipeline (model + preprocessing). |
| Extra: Wisconsin breast cancer & California housing case studies | covered | End-to-end hands-on demos across classification (breast cancer CSV) and regression (California housing) tying together metrics, complexity, tuning, persistence, and selection. |
