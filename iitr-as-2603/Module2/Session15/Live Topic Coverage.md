| topic/sub topic | status | remarks |
|---|---|---|
| Data Leakage: Definition, Scenarios & Guards — Define data leakage and why it creates misleadingly high performance; cover common scenarios (preprocessing before split, target-related features, test-set statistics in transforms) with a brief link to the previous session workflow; establish the guard checklist: split first, fit preprocessing on training data only, isolate test data, and simulate real-world prediction conditions. | covered | “Silent cheater” framing; misleading performance tied to last session’s min–max example; leakage via mean imputation and feature scaling (test statistics learned before split); split-first workflow, `fit` on train / `transform` on test, preprocess features (X) not outputs (Y); strong link to Session 14 notebook; target-derived feature leakage as a named scenario was not discussed |
| Introduce Class Imbalance — Explain imbalance using intuitive examples (e.g., fraud detection, rare diseases); show how datasets can be skewed toward one class. | covered | Credit-card fraud detection walkthrough; diabetes / health detection (minority vs majority); ~99% vs ~1% transaction example |
| Understand the Impact of Imbalance — Demonstrate why models become biased toward the majority class and why accuracy becomes unreliable on skewed data. | covered | Majority-class bias; dummy classifier predicting all “not fraud”; accuracy paradox (99% accuracy, zero frauds detected) |
| Introduce Precision and Recall (Conceptual Only) — Explain using real-world scenarios without formulas; focus on intuition of false positives and false negatives. | covered | FP/FN with fraud and medical false-alarm examples; precision vs recall intuition (predict vs detect); went beyond metadata—confusion matrix, TP/FP/FN/TN, and formulas (TP/(TP+FP), TP/(TP+FN)) |
| Handling Imbalance: Resampling & Synthetic Data — Introduce oversampling and undersampling with simple intuition and trade-offs; explain synthetic balancing (e.g., SMOTE intuition) as generating artificial but similar minority examples; keep conceptual, no deep implementation. | covered | SMOTE (synthetic minority oversampling) explained with intuition and notebook demo; oversampling named in SMOTE and post-lecture quiz; undersampling trade-off (losing useful majority-class information) covered mainly in quiz debrief, not as a dedicated lecture segment |
| Cross-Validation: Why & How (Conceptual) — Explain why a single train-test split is unreliable; introduce multiple splits, how performance varies across folds, and why averaging results gives better confidence; diagram or verbal walkthrough preferred over a full coding lab. | covered | Mock-paper and multi-round interview analogies; multiple train–test splits and averaged performance; no coding lab (deferred to later ML sessions) |
| Connect Leakage, Imbalance, and Evaluation — Reinforce how improper handling of these concepts leads to incorrect conclusions about model performance. | covered | End-of-session recap tying leakage guards, imbalance, accuracy limits, precision/recall, SMOTE, and cross-validation; quiz reinforced balancing test set / oversampling pitfalls |
| *(Extra — not in metadata subtopic list)* Confusion matrix and classification report | covered | Extended evaluation beyond conceptual precision/recall; used to show why accuracy hides poor fraud detection |
| *(Extra — not in metadata subtopic list)* F1 score | covered | Harmonic mean of precision and recall; mentioned alongside baseline vs SMOTE comparison |
| *(Extra — not in metadata subtopic list)* Logistic regression baseline with SMOTE demo | covered | Brief coded walkthrough (train–test split, baseline model, SMOTE resampling) for illustration, not full implementation depth |
| *(Extra — not in metadata subtopic list)* Mentimeter / post-lecture quiz | covered | Questions on leakage, oversampling on test set, undersampling trade-offs, and related pitfalls |
