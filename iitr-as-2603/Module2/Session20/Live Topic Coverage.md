| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Unsupervised Basics – learning patterns without labeled targets | covered | Covered in depth: contrast with supervised learning (no labels/Y), pattern/cluster discovery, kindergarten-fruits analogy, finding similarities without outputs, and note that there is no train/test split concept in unsupervised learning. |
| K-Means Clustering – grouping similar data points for segmentation | covered | Covered with retail customer-segmentation use case, centroid intuition, real-world applications (recommendations, Spotify/music features), and hands-on `KMeans` on sample customer time/money-spent data with cluster labels. |
| K-Means Workflow – assigning points to clusters and cluster centers | covered | Algorithm steps explained with animations: choose K, random centroid init, assign points to nearest centroid, recalculate centroids, repeat until convergence; reinforced in code via `kmeans.fit(X)` (no Y). |
| PCA – reducing many features into fewer components | covered | Contextual theory on dimensionality reduction, compressing many features while retaining information, `StandardScaler` + `PCA(n_components=2)` + `fit_transform` on 10-feature simulated data; also discussed MNIST 784→2/3 components conceptually. |
| PCA Visualization – projecting high dimensional data into 2D for visualization | covered | Explicit 2D scatter of PC1 vs PC2 after reduction; motivation that humans cannot plot beyond 2–3 dimensions; MNIST/Google experiment demo for visualizing reduced dimensions; PCA-reduced data then used for clustering visualization. |
| Extra: Choosing optimal K (elbow method, inertia/WCSS) | covered | Significant time on domain knowledge vs elbow method, inertia as within-cluster sum of squares (WCSS), elbow plot in code (K=1..10), and selecting optimal K=3 in demo. |
| Extra: Unsupervised learning applications (recommendation systems, anomaly detection) | covered | Additional concepts taught with meaningful time: Netflix-style recommendations as clustering similar items, and brief anomaly-detection mention as another unsupervised problem type. |
| Extra: Cluster interpretation and business storytelling | covered | Post-clustering interpretation of segments (time vs money spent profiles) for customer groups after K-means. |
| Extra: PCA followed by K-means on transformed data | covered | End-to-end demo chaining `df_pca` reduced features with optimum-K K-means clustering on PCA output. |
