| Topic / Sub-topic | Status | Remarks |
| --- | --- | --- |
| Understand that LLMs can run locally on your own machine, not only through paid cloud services | Covered | Contrasted OpenAI/Gemini/Claude (cloud, paid, API key) with open-source models via Ollama; reasons to run locally (cost, privacy, learning, speed). |
| Install Ollama on your operating system and verify that the setup works | Covered | Walked through ollama.com download, OS install steps, and verification with `ollama -v` (version check). |
| Pull a light local model (for example 0.5B or 1B parameter models) and run a simple prompt from the CLI | Covered | Demonstrated `ollama run` pulling a small Qwen model (~1.1 GB); interactive CLI prompt (e.g., OpenAI vs local model). 0.5B/1B sizes not named explicitly—small models shown by disk/RAM footprint instead. |
| Explain why learners should avoid pulling heavy models on regular laptops | Covered | Used large models (e.g., DeepSeek, multi‑GB/100+ GB RAM examples); warned about slow/hanging laptops and impractical downloads for learning. |
| Describe the trade-offs of very small local models, including limited training data and a higher chance of hallucination | Covered | Quality vs size trade-off (fewer parameters, less world knowledge); small models may hallucinate due to limited context—do not blindly trust responses. “Limited training data” phrasing not used verbatim. |
| Write a short Python script that calls a local model through the Ollama API and prints the response | Covered | Built `app.py` using `requests` POST to `localhost:11434/api/chat` with model, messages, and `stream=false`; printed response content. |
| Create an Ollama account and generate a free-tier Ollama API key | Covered | Account creation on Ollama; API key from account → settings → keys; set via `export OLLAMA_API_KEY=...`. Free-tier label not emphasized in detail. |
| Update the same Python script to call Ollama Cloud by changing the API key and host settings | Covered | Second script (`app2.py`): host `https://ollama.com/api/chat`, Bearer token in headers, `os.environ.get` for API key—same payload pattern as local. Shown as a separate file, not a single in-class edit of one script. |
| Compare local light-model output with Ollama Cloud output on the same prompt | Not covered | Same prompt reused in demos (e.g., REST API explanation) but no side-by-side comparison of local vs cloud outputs on identical input. |
| List the main things Ollama can do, including chat, text generation, and embeddings | Covered | Recap listed chat, text generation, embeddings, audio/video generation; live focus was chat API—embeddings not demonstrated hands-on. |
| Identify the main model types available through Ollama, including text, vision, audio, and video | Covered | Browsed model library: text-only vs multimodal (text, image, audio, video); filters for thinking, vision, code models. |
| Finish the session with one working Python script that can call either local Ollama or Ollama Cloud | Not covered | Local and cloud scripts demonstrated separately; unified script with `mode` (local/cloud) left as homework—reference code in notes. |
| **Extra concepts taught (beyond listed subtopics)** | Covered | OpenAI API keys via environment variables (recap); when to choose local vs cloud; Ollama local host/port (`11434`); CLI commands (`ollama list`, `ollama rm`, `ollama ps`, `ollama stop`); hardware impact (CPU, GPU, RAM, bandwidth); streaming vs non-streaming; cloud model “pull” without local weight download; fine-tuning/files-on-disk mention; 5‑min install break for learners. |
