| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Extend agent prompts with placeholders that admit rolling conversational history across invocations. | covered | Explained `MessagesPlaceholder` in `ChatPromptTemplate`, wired `chat_history` with `optional=True`, contrasted with `agent_scratchpad`, and showed passing `chat_history` on `AgentExecutor.invoke`. |
| Demonstrate context continuity across multiple turns where later turns depend on earlier user-supplied facts. | covered | Built customer-support agent demo: turn 1 shares order ID, turn 2 asks status without repeating ID; agent recalls ID and calls `get_order_status` correctly after history append. |
| Diagnose and correct representative defects in history wiring for executor-based agents. | covered | Reproduced failure when placeholder was defined but history was not appended; fixed with manual `chat_history.append` (human then AI). Closed with common debug points: wrong placeholder variable name, missing append, wrong message order, shared memory across sessions. |
| Justify when message-history attachment changes answer quality versus a stateless agent baseline. | covered | Compared with-memory vs without-memory runs; showed stateless behavior by passing empty `chat_history` (`langchain_agent_stateless.py`); order-ID recall failed without history and succeeded with it. |
| Extra concepts taught (beyond listed subtopics) | covered | `RunnableWithMessageHistory` + `InMemoryChatMessageHistory` with session-scoped `store`; manual vs automatic history updates; rolling history via `n_messages`; when to persist chat history to a database; `agent_scratchpad` vs `chat_history` for tool-calling agents; session/conversation ID isolation (ChatGPT tab analogy). |
