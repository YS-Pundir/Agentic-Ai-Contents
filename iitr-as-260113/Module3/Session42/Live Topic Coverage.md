| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Define structured evaluation cases with explicit expected behaviors for tools grounding and refusal. | covered | Built `evaluation_cases.json` with per-case `id`, `input`, and expected fields: `must_use_tools`, `forbidden_tools`, `must_cite_doc_ids`, `must_contain`, and `should_refuse`. Walked through refund policy, pause policy, placement guarantee, refund-amount math, private-phone refusal, out-of-scope (forbidden tools + refuse), and enrollment-transfer refusal cases. Explained grounding via correct tool calls, correct document citation/retrieval, and refusal expectations. |
| Implement consistent logging of inputs retrievals tool traffic and final responses across evaluation cases. | covered | Added trace/logging in `agent_app_evaluation.py` using `contextvars` so each test case gets an isolated trace; `record_event` captures event type, payload, and timing. Logged tool calls (with per-tool latency in ms), retrievals, and final responses into per-case trace JSON files under a `traces/` directory. Runner reads `tool_calls` and retrieved document IDs from traces for evaluation. |
| Classify qualitative outcomes and isolate lowest-performing trajectories for focused follow-up work. | covered | Implemented `evaluate_case` to compare expected vs actual (tools, forbidden tools, citations, content, refusal). Scored cases with a sample formula (`max(0, 1 - 0.25 × failure_count)`); classified failures (e.g. runtime, missing tool, forbidden tool). Wrote aggregated output to `results.csv` (status, score, failure type, tools used, retrieved docs, latency, final response). Live run of 6 cases (3 passed, 3 failed) printed lowest-performing cases and linked failures to trace files for follow-up. |
| Explain how the harness design would extend if new tools or corpora are added without rewriting the entire evaluation philosophy. | covered | Explained that new functionalities, policy documents, tools, and scenarios require adding/updating JSON test cases (e.g. ~10 cases per feature) while the same harness flow remains: JSON cases → runner → trace capture → expected-vs-actual comparison → `results.csv` + per-case traces. Adding forbidden/must-use tools is done by extending case definitions, not rewriting the evaluation runner logic. |
| Extra concepts taught (beyond listed subtopics) | covered | Why agent evaluation cannot rely on input/output alone (multi-step actions: think, tool calls, retrieval, refusal); step-by-step evaluation for complex agent flows (refund example with order status + policy retrieval); logs vs print statements and trace as step-by-step request history; semantic/similarity search for refusal/input matching (not exact string match); sample refusal phrases list; latency measurement as an evaluation dimension; production-ready evaluation demo; brief module-exam/MCQ logistics and recap of prior LangChain/RAG/agent work. |
