| Topic / Sub-topic | Status | Remarks |
| --- | --- | --- |
| Define LangChain in one sentence — an open-source framework for building applications powered by LLMs | Covered | Official-style definition taught (~9:01–13:04): open-source framework connecting models, prompts, tools, memory, retrievers, and workflows for LLM-powered applications. |
| Explain why raw LLM API calls become hard to maintain when prompts, memory, tools, retrieval, and chaining are needed | Covered | Contrasted “just call the LLM” vs real apps needing pre/post-LLM steps; FastAPI-style framework analogy; orchestration layer, composability, reuse, and provider/database/tool migration (~10:31–24:15). |
| Place LangChain on the agentic stack between the model provider and the application | Covered | User → UI/backend → LangChain orchestration layer → LLMs, DBs, tools, embeddings, APIs; developer talks to LangChain rather than each integration manually (~15:47–24:31). |
| Introduce the LangChain mental model — Runnables composed into chains | Covered | Applications built by connecting small reusable components (runnables); invoke/stream/batch mentioned briefly; prompt → LLM → agent → tool → DB flow (~26:31–29:26, ~27:12–28:11). |
| Explain how chains, agents, tools, memory, and retrievers fit together in a single-agent workflow | Covered | Six core components walked through with an end-to-end narrative (models, prompts, chains, memory, indexes/retrieval, agents); simplest app = chain without agent/tools (~27:12–27:28, ~40:11–2:11:00). “Single-agent workflow” wording not used explicitly. |
| Describe how PromptTemplate and ChatPromptTemplate keep prompts reusable and variable-driven | Covered | PromptTemplate as blueprints with placeholders/curly-bracket variables; hard-coded vs template contrast; live `PromptTemplate.from_template` + `.format()`. ChatPromptTemplate explained for chat models (list of messages) but not demonstrated in code (~45:20–47:18, ~1:35:39–1:55:38). |
| Build or follow a first LCEL chain as PromptTemplate → LLM with string output handling | Covered | `app2.py`: `prompt \| llm \| StrOutputParser`, `chain.invoke()` with template variables; string output parser returns content only vs full response object (~2:0:32–2:9:30). |
| Compare hardcoded prompts vs templated prompts on the same task | Covered | `app1.py` hard-coded SQL-indexes prompt vs templated variables (topic, audience, tone, limit); conceptual comparison before coding (~27:35–28:01, ~1:45:32–1:55:09). |
| Note where broader framework choices were introduced in Module 1 and defer multi-agent framework comparison to Module 4 | Not covered | Referenced an earlier “definitions of X/Y/Z frameworks” class (~7:33–7:46) but did not name Module 1 or defer multi-agent framework comparison to Module 4. |
| **Extra concepts taught (beyond listed detailed subtopics)** | Covered | LangChain vs raw OpenAI (same complexity for trivial invoke); `langchain`, `langchain-openai`, `langchain-core` installs/imports; provider abstraction and plug-and-play switching; orchestration vs manual integrations; runnable examples (LLM, retriever, tool, chain); chain definition and LLM chain pattern; sequential, composite, and parallel chaining (incl. YouTube-style parallel example); memory types and context-aware agents/chains; indexes for RAG/document retrieval (book-index analogy); agents as dynamic tool-selecting components; example selectors (linked to few-shot); middle layer extracting user fields into templates; `invoke` / `stream` / `batch` on chains (stream deferred to next class); venv setup and code pushed to GitHub. |
