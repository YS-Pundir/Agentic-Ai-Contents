| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Select remediation strategies by defect category informed by evaluation artifacts. | covered | Taught failure classes (wrong tool selection, missing tool call, bad tool arguments, weak retrieval, hallucinated response, over/under-refusal, excessive tool calling, slow response) and explicit remediation-by-defect mappings (e.g. improve tool name/description/system prompt for wrong tool; enforce “must call this tool”; tune K, chunk size, search type for weak retrieval; max iterations for loops; reduce unnecessary retrievals/caching for latency). Prior-session evaluation artifacts (`eval.json`, `results.csv`, traces) are recapped as the diagnostic source, but live remediation selection is demonstrated via failure-class diagnosis rather than walking through artifact-driven triage in this session. |
| Quantify quality movement using the same harness before and after targeted changes to prompts tool descriptions or retrieval parameters. | not covered | Quality metrics are defined conceptually (response/tool/argument/retrieval accuracy, groundedness, refusal accuracy, latency, token utilization) and prior-session harness is referenced. Live work shows qualitative before/after on a RAG query after chunk-size/overlap changes and brief prompt/tool-patch examples, but the evaluation harness is not re-run and no before/after scores or CSV metrics are produced in this session. |
| Relate configuration changes to resource consumption (token usage and latency) for at least one representative workflow. | covered | Cost–latency trade-off discussed with concrete levers (higher K → more context/tokens; larger prompts/chunks → higher token cost; better LLM/context window → cost vs quality). Observability/monitoring for latency thresholds and alerts covered. Representative LangChain RAG workflow demo (`langchain_rag_ingest.py` + `langchain_rag_app.py`) shows how chunk size/overlap changes affect retrieval quality and answer correctness on the same query. |
| Decide when incremental gains justify stopping iteration against a stated quality bar for the module. | not covered | Instructor advises trying multiple chunk/K/search combinations (e.g. 10–20 iterations) and accepting a cost–latency budget, but does not define a module quality bar or criteria for when to stop iterating versus continuing patches/tuning. |
| Extra concepts taught (beyond listed subtopics) | covered | Debugging vs traditional I/O apps; final answer vs trajectory debugging; prompt patching, tool patching, retrieval tuning as terms; hallucination and refusal failure modes; automated observability/alerting for latency, memory, and failures; hands-on chunk-overlap mismatch demo explaining why wrong chunking breaks retrieval; module recap mind map; preview of next module (deployment, multi-agent, N8N, CrewAI, AutoGen). |
