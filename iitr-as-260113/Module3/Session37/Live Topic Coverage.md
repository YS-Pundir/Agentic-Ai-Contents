| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Author LangChain-native tools with typing and descriptions that support reliable model-driven selection. | covered | Live implementation with `@tool`, Pydantic `BaseModel` / `args_schema`, type hints vs enforcement, field descriptions (e.g. order ID format), `Literal` for constrained inputs, snake_case tool names, and production guidance on clear tool names and descriptions so the model can select tools reliably. |
| Interpret model-emitted structured tool calls and relate them to intended runtime behavior. | covered | Explained model output as `AIMessage` with `content` and `tool_calls`; walked through extracting tool name, id, and arguments from each call; built `execute_tool_call_safely` to map calls to the correct tool via a `tools_by_name` dictionary and `invoke`; related behavior to order-status vs refund scenarios. |
| Orchestrate a manual tool-feedback loop that arrives at a coherent final model response after tool execution. | covered | Implemented `run_customer_support_agent` with system + user messages, `model_with_tools.invoke`, appending `AIMessage` and `ToolMessage` to the message list, iterating `tool_calls` (including multiple calls), `max_steps` loop (up to five iterations), and live runs showing step 1 tool execution and step 2 final natural-language response for order-tracking and refund queries. |
| Apply error-containment patterns so tool failures surface as recoverable signals rather than hard crashes. | covered | Used try/except around `tool.invoke`, returned `ToolMessage` with structured JSON errors (e.g. unknown tool, validation failures, order not found, payment not completed); taught error-containment patterns on iPad—structured error objects, catch tool exceptions, validate arguments before invocation, keep tools minimal/deterministic, and limit tool output size. |
| Diagnose common tool-selection and argument faults using traces from a controlled query set. | covered | End-of-class diagnosing segment on correct tool selection vs hallucination; stressed system-prompt clarity, tool descriptions exposed via `bind_tools`, and pre-production testing on ~10–30 scenarios; demonstrated with controlled queries (“where is my order…”, refund ticket) and printed step-wise tool-call / response trace during live execution. |
| Extra concept: Conceptual recap — tools, LLM-only limits, and tool-call workflow. | covered | Spent time on what tools are, when LLMs need external tools/APIs/DBs, and the flow: LLM chooses tool → application invokes tool → tool result returned to LLM (not directly to user) for a final answer. |
| Extra concept: `bind_tools` and `init_chat_model`. | covered | Created model with `init_chat_model` (model name, temperature) and bound the tool list via `model.bind_tools(tools)` so the model knows available tools before invocation. |
| Extra concept: Mock/fake databases for the demo. | covered | Used in-memory dicts for orders and policies instead of setting up SQL/vector DB, to keep focus on tool-calling mechanics. |
| Extra concept: Multi-tool customer-support scenario (`get_order_status`, policy lookup, `create_refund_ticket`). | covered | Built and wired multiple tools with validation rules (e.g. refund only when order exists and payment completed); showed different tools selected for different user queries. |
| Extra concept: Production-oriented tool design practices. | covered | Discussed production-ready tools, meaningful return formats (JSON/string), avoiding vague outputs, and why tool quality matters for debugging failures in large LangChain/LangGraph apps. |
