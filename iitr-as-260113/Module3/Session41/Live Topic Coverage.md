| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Design retriever-backed tools whose contracts support accurate arbitration alongside non-retrieval tools. | covered | Introduced tool arbitration and why tool names/descriptions must state what the tool does, when to call it, and what need it fulfills. Built `create_retriever_tool` as `course_policy_tool` (RAG-backed) alongside `@tool` `get_ticket_status` (fake ticket DB). Walked through routing examples: refund policy → retriever; ticket status → auxiliary tool; refund ticket → possibly both; IPL 2025 → no tool/RAG (out-of-domain). Prompt rules and live run showed correct tool selection per query type. |
| Demonstrate multi-turn document Q&A that combines conversational memory with retrieval-backed answers. | covered | Wired `MessagesPlaceholder` for `chat_history` and `agent_scratchpad` in the prompt; implemented `ask_agent()` with manual `HumanMessage`/`AIMessage` append to persist turns. Live demo ran sequential queries (refund policy, then ticket status) and a combined ticket-status + refund-policy query through the same agent. Instructor explained follow-up memory (ticket ID in turn 1, status in turn 2); that context-dependent follow-up was described rather than shown as a separate live demo. |
| Appraise integrated agent behavior against a compact evaluation set spanning in-domain out-of-domain and tool-first scenarios. | covered | Built and executed a compact EvalPack: iterated test cases with name, input, expected tool, expected keywords, and failure type; live run walked refund-policy (retrieval/in-domain) and ticket-status (tool-first) cases end-to-end. Suggested post-class validation of expected keywords and tool calls. Out-of-domain refusal was taught in the arbitration block (IPL example + prompt guardrails) but was not clearly shown as an executed EvalPack case in the live run. |
| Interpret failure signatures (wrong tool weak retrieval over-refusal) at a level sufficient to prioritize fixes in the testing block. | covered | Covered four failure modes with fixes: wrong tool call → improve tool descriptions and recheck prompt; weak retrieval → embeddings, chunk size/overlap, top-k, alternate search (e.g. MMR); over-refusal → overly strict prompt/restrictions and weak tool descriptions; missing memory → chat history not appended, agent scratchpad issues. Linked failure types to EvalPack fields (e.g. weak retriever / missing retrieval on refund-policy case). |
| Extra concepts taught (beyond listed subtopics) | covered | Agentic RAG / tool-based RAG vs standalone RAG recap; integrated LangChain workflow (`create_tool_calling_agent`, `AgentExecutor`, verbose, `max_iterations`); agent scratchpad for tool observability; course-support bot use case; document metadata in vector ingest; module mind-map and topic checklist; storing chat history in a database for production (brief mention at close). |
