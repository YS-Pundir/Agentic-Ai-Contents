| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Ingest and segment a document corpus using LangChain loaders and chunking aligned with the established Module 2 corpus design. | covered | Built e-commerce policy corpus (`create_corpus.py`: return/refund/shipping/warranty/cancellation `.md` files); ingested via `DirectoryLoader` + `TextLoader` with `**/*.md` glob and UTF-8 encoding; split with `RecursiveCharacterTextSplitter` (chunk_size=800, chunk_overlap=120, `add_start_index=True`) in `ingest.py`. Module 2 corpus design was not named explicitly, but the same policy-document corpus pattern from prior RAG work was reused. |
| Embed and persist vectors in Chroma through LangChain so retrieval remains reproducible across runs. | covered | Used `OpenAIEmbeddings` and `Chroma.from_documents` with `persist_directory`, collection name `e-commerce_policy_docs`, and optional `shutil.rmtree` cleanup before re-ingest; explained persist = save locally for reuse; `rag_app.py` reloads persisted Chroma without re-embedding. Live demo created `chroma_db` collection successfully. |
| Construct an LCEL retrieval-augmented answering chain that conditions generation on retrieved passages. | covered | Built full LCEL RAG chain in `langchain_rag_app.py`: `RunnablePassthrough` for question → retriever context → `ChatPromptTemplate` → `ChatOpenAI` → `StrOutputParser`; configured retriever with similarity search and `k=3`; executed end-to-end and got grounded answer with source citation (`return_policy.md`). |
| Critique grounding quality by contrasting outputs with versus without retrieval on representative queries using instructor-supplied comparison criteria. | not covered | Demonstrated grounded vs unanswerable behavior (electronics return query with citation; baby-items query answered “I don’t know based on the provided documents”) and discussed prompt guardrails, but did not run a structured with-retrieval vs without-retrieval comparison on the same queries or apply instructor-supplied comparison criteria. |
| Extra concepts taught (beyond listed subtopics) | covered | RAG theory recap and typical flow (loader → splitter → embeddings → vector DB → retriever → LLM); fine-tuning vs RAG trade-offs; chunkification analogies (Netflix/YouTube); `chunk_size`/`chunk_overlap` theory; OpenAI embedding models (small vs large); Chroma collections; one-time ingest vs reuse; re-ingestion after source-doc edits; citation/sourcing in answers; production workflow (`create_corpus` → `ingest` → `rag_app`); `RunnablePassthrough` role; guardrails and “use only retrieved context” prompt design. |
