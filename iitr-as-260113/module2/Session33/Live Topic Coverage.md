| topic/sub topic | status | remarks |
|---|---|---|
| Recap How APIs Work — Briefly revisit the previous session: what an API is, the request-response cycle, HTTP methods, status codes, and JSON as the data format; establish this as the foundation for everything in this session | covered | API as a contract between systems; client–server and request–response cycle; GET/POST/PUT/PATCH/DELETE; status codes (200, 404, 401, 5xx families); JSON as the interchange format; microservices and third-party payment (Razor Pay) used as extended recap |
| Understand Function Calling in AI Agents — Explain that agents can call external functions/tools when they need to take action; the agent decides what tool to use, the system executes it, and returns the result back to the agent via structured output | covered | Tools vs functions used interchangeably; model chooses a tool and emits structured arguments; application validates and runs the call; results returned to the model for the final answer; full step-by-step agentic tool-call flow; model does not call tools directly—the agent/runtime executes |
| See How Agents Use APIs as Tools — Show that every external tool an agent uses (search, calculator, database, email, etc.) is accessed via an API call — the agent sends a request and receives a JSON response, exactly like the previous session | covered | Travel, weather, database, and similar services framed as tools; third-party and in-app database access via APIs; JSON tool responses fed back into the agent loop |
| Understand How Applications Connect to Databases — Introduce the idea that applications (and agents) do not talk to databases directly — they go through a connection layer (drivers/connectors); explain the role of Python as the application layer that sits between the agent and the database | covered | Python as the application layer; database drivers/connectors; ORM introduced as a mapping layer before the live build |
| Execute SQL Queries from Python — Explain how SQL queries are triggered from Python code rather than manually typed in a SQL tool; show the concept of running a SELECT or INSERT from within a Python script and receiving results as structured data | covered | Manual SQL strings in Python discussed as possible; live work used SQLAlchemy sessions with `add_all`/`commit` for inserts and `select`/`where` for reads—ORM/expression API rather than hand-written SQL in the demo |
| Understand What Pydantic Does and Why It Matters — Explain Pydantic as a Python library for data validation and structure; show how it defines what a piece of data should look like (field names, types, constraints) — similar to how JSON has a shape, Pydantic enforces that shape in Python | covered | Short recap plus `BaseModel`, `Field`, required vs optional fields, type and range checks, and `ValidationError` handling in the product-search example |
| Connect Pydantic to APIs — Show that Pydantic models are used to validate incoming API request data and to structure outgoing API responses; this is how Python, APIs, and structured data stay consistent with each other | not covered | Pydantic applied to agent/tool argument validation only; no explicit walkthrough of HTTP request-body or response-body modeling for APIs |
| Understand What SQLAlchemy Does and Why It Matters — Introduce SQLAlchemy as an abstraction layer over SQL; explain that instead of writing raw SQL strings, Python code defines and queries database tables using Python objects — making it safer, more readable, and less error-prone | covered | Library (not a database); Core vs ORM; drivers and ORM table/class mapping; live SQLite engine, declarative base, `Product` model, and metadata `create_all` |
| See How Python, SQL, and Pydantic Work Together — Walk through a complete flow: agent triggers a tool → Python function runs → Pydantic validates the input → SQLAlchemy queries the database → SQL executes → result returned as a Pydantic model → agent receives structured JSON output | covered | End-to-end `search_product` / simulated `agent_flow` demo: validate dict input with Pydantic, query via SQLAlchemy, return product data; hard-coded tool arguments in place of live LLM routing; results returned as Python objects/lists rather than a Pydantic response model |
| Solidify the Full Stack Mental Model for Agents — Tie together both sessions: agents communicate via APIs (Session 32) and use Python + Pydantic + SQL as the backend layer to access, validate, and return data (Session 33); students should leave able to trace a full agentic action from trigger to data retrieval to response | covered | APIs-for-tools plus Python/Pydantic/SQLAlchemy backend synthesized in discussion and demo; prior module sessions referenced generically, not as “Session 32/33”; OpenAI/LLM wiring discussed as a follow-on, not completed live in class |
| *(Extra — not in metadata subtopic list)* ORM (object–relational mapping) concept | covered | Classes mapped to tables; attributes to columns; contrasted with hand-maintaining large schemas |
| *(Extra — not in metadata subtopic list)* External tool authentication | covered | Brief note that real integrations usually require auth; not implemented in the demo |
| *(Extra — not in metadata subtopic list)* OpenAI client and LLM-driven tool selection | covered | Closing logic for passing tool lists and user queries to an LLM; students asked to integrate the API themselves |
| *(Extra — not in metadata subtopic list)* Tool metadata (name and description) for LLM routing | covered | Descriptions matter more than arbitrary function names when the model chooses a tool |
| *(Extra — not in metadata subtopic list)* SQLite e-commerce sample database and virtual-environment setup | covered | `agent_tool_call` project, venv, dependency install, in-folder SQLite DB, sample product seed data |
| *(Extra — not in metadata subtopic list)* Converting ORM result objects for agent/JSON consumption | covered | Product instances as list elements; memory-reference vs field access; shaping output for tool responses |
| *(Extra — not in metadata subtopic list)* Module transition preview (LangChain / LangGraph) | covered | Mentioned as the next module’s agent-framework focus |
| *(Extra — not in metadata subtopic list)* Post-class quiz / recap | covered | Short assessment after code and notes were shared |
