| Topic / Subtopic | Status | Remarks |
|---|---|---|
| Understand How Software Is Structured — and Why Agents Need to Know This (layered architecture: FE, BE, DB framed for agents) | Covered | Clearly introduced FE/BE/DB architecture from the lens of why agents need to understand these systems; travel agent example used to connect to real-world agentic use cases |
| Understand the Role of Each Layer (frontend = user-facing, backend = logic, database = storage; agents interact at backend via APIs) | Covered | Explained frontend as "what user sees", backend as the "brain/chef", database as data store; explicitly stated agents are written in the backend layer |
| Visualize the Full Request Flow: FE → API → BE → API → DB | Covered | Walked through complete flow with Amazon/Swiggy orders example — user action → API call to backend → backend queries DB → DB returns data → backend sends API response → displayed to user |
| Introduce APIs as the Universal Communication Layer (API as structured contract between two software components) | Covered | Defined APIs via multiple definitions: "bridge between agent's brain and real-world software", "contract between two software components defining what request can be sent and what response will be received"; OpenAI client example used |
| Understand REST APIs and HTTP Methods (GET→Read, POST→Create, PUT/PATCH→Update, DELETE→Remove) | Covered | REST explained as a design convention for structuring APIs around resources; all five methods (GET, POST, PUT, PATCH, DELETE) explained with CRUD mapping and practical examples |
| Understand the Structure of a Request and Response (URL, method, headers, body; status code, response body in JSON) | Covered | Broke down API call into URL/endpoint, HTTP method, headers (auth token), and request body; showed this via browser Inspect Element / Network tab on Flipkart live |
| Decode HTTP Status Codes (200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Internal Server Error) | Covered | All listed status codes explained; additionally 403 Forbidden was covered (accessing admin portal without permission); explained 2xx = success, 4xx = client error, 5xx = server error pattern |
| Understand JSON as the Data Format for APIs (structure, key-value pairs, Python dict parallel, json.loads(), json.dumps()) | Covered | JSON defined as lightweight text format; structure demonstrated (objects, arrays, key-value pairs, nested objects, data types); Python dict parallel explicitly drawn; json.loads() and json.dumps() demonstrated live in code with an order JSON example |
| Understand That AI Models Also Respond via APIs (calling OpenAI/Gemini = same API request-response pattern) | Covered | Explained that calling OpenAI (client.responses.create()) is also an API call like any other; mentioned migrating from OpenAI to Gemini follows same pattern with different contract; API key and authentication discussed |
| Trace an Agentic API Call End to End (agent → POST to OpenAI → JSON response → agent reads and decides next action) | Partially Covered | Showed OpenAI API call flow from Python (install library, create client, pass model/instructions/prompt, get response); however, a dedicated step-by-step trace of an agentic end-to-end call (showing how agent reads JSON response and decides next action) was not demonstrated as a focused walkthrough — it was referenced briefly at the end |
| **Extra: MCP (Model Context Protocol) concept** | Extra Topic | Instructor briefly introduced MCP as an abstraction layer between agents and LLM APIs to avoid tight coupling when switching providers (e.g., OpenAI to Gemini); noted it is a separate big topic not part of this session |
| **Extra: Live browser network tab inspection** | Extra Topic | Demonstrated live on Flipkart — opened Inspect Element → Network tab to show real API requests, request headers, status codes, and payload in action |
| **Extra: End-of-class quiz** | Extra Topic | 5-question quiz conducted covering: database layer responsibility, POST method, 201 status code, 500 status code meaning |
