| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Load plain-text or PDF documents into a consistent in-memory or file-based corpus. | covered | Built an in-memory sample corpus (policy-style text) for the manual chunking lab; live-loaded Tesla SEC PDF(s) from a folder via LangChain/PyPDF (one-line load). End-of-session recap explicitly included plain text and PDF loading. Plain-text file loading was mostly via in-memory/corpus definition rather than a separate file-loader walkthrough. |
| Apply chunking strategies with justified chunk size and overlap for a sample dataset. | covered | Taught fixed-size character splitting with overlap as the primary RAG strategy; justified why chunk size cannot be too large (one chunk = whole doc) or too small (one character); demonstrated overlap (e.g. 16 chars on 512/550-size chunks) to preserve cross-boundary context; ran sample policy corpus chunking and Tesla demo (512 size, 16 overlap → 351 chunks). Also discussed page-based and sentence-based chunking as alternatives; semantic chunking covered briefly as extra (not in syllabus). |
| Attach lightweight metadata (source id; page) to chunks for traceability. | covered | Explained tagging/metadata workflow (source ID = filename, page number, chunk index); simulated and stored metadata on policy sample chunks; verified via collection peek/get (filename, page, chunk index). Clarified metadata is stored as labels, not embedded. Tesla end demo intentionally omitted metadata attachment (“keeping it simple”) but traceability concept and hands-on metadata on the sample dataset were covered. Extra: optional metadata (year, category) for filter-style retrieval. |
| Persist chunked documents into the vector store created in the prior session. | covered | Persisted chunks in ChromaDB using sentence-transformer embeddings and collection upsert (documents, metadata, embeddings) on the policy sample; connected to prior-session FAQ/Chroma flow. Tesla demo used LangChain `Chroma.from_documents` on 351 chunks with the same embedding model concept. Inspected storage with peek/get/count. |
| Extra: End-to-end RAG workflow recap (load → chunk → embed → store → retrieve) | covered | Recapped prior sessions (prompts, Ollama, embeddings, retriever, top-k retrieval) and framed today as document-preparation depth before full RAG assembly on Friday. |
| Extra: Semantic chunking (advanced) | covered | Brief but substantive explanation: split into sentences, group by embedding similarity; noted as computationally expensive and not the session’s primary strategy. |
| Extra: ChatGPT-style document upload vs true RAG / in-memory RAG | covered | Contrasted treating whole uploads as one user message (no chunking/vector DB) with proper chunked RAG and mentioned in-memory RAG as a variant. |
| Extra: LangChain for load/chunk/store simplification | covered | Showed Tesla demo using LangChain wrappers (one-line load, one-line chunk, simplified Chroma persistence) vs manual base-Python approach in the policy lab. |
| Extra: Vector DB indexing and admin roles (HNSW, IVF, etc.) | covered | Q&A on similarity indexes for nearest-neighbor search and parallels to traditional DB administration. |
