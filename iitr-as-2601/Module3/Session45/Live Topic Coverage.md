| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Store prompts and tool configs in versioned files or a simple registry pattern. | covered | Taught file-based versioning (`V1_system.txt`, `V2_system.txt`) and a folder layout (`prompts/<agent>/` with versioned system messages, `eval_questions.txt`, and config files); config files explained for model name, temperature, and max tokens; registry pattern introduced as a Python dictionary/JSON single source of truth bundling prompt text, configs, and tool metadata; live demos for both file-based storage and registry lookup (`registry[agent_name][version]`). |
| Compare two prompt versions against the same eval questions (qualitative). | covered | Live ShopEasy sentiment-analysis demo: V1 fails a prompt-injection user message while V2 (with guardrail instructions) succeeds on the same input; `eval_questions.txt` explained as the fixed test set for comparing versions; qualitative evaluation criteria discussed (correctness, injection resistance, refusal behavior); instructor emphasized evaluation is required to know which version performs better. |
| Explain HTTP rate limits and implement exponential backoff on API errors. | covered | HTTP rate limits explained (provider caps on requests/tokens per minute); HTTP 429 errors demonstrated; OpenAI and Groq dashboard rate-limit tiers referenced; exponential backoff and retry strategy taught with IRCTC-refresh analogy; `tenacity` package introduced with `@retry`, `wait_exponential` (1s → 2s → 4s → 8s), and `stop_after_attempt`; live Colab demo simulating flaky API / HTTP 429 before successful call; applied to Groq `chat.completions.create`. |
| Log retry events so failures are visible during development. | covered | `before_sleep` callback used to log each retry attempt; demo output showed messages like “Attempt number one, simulated rate limit warning, waiting for one second”; instructor noted Tenacity handles logging automatically so failures are visible during development rather than silently crashing. |
| Extra: Prompt injection attack | covered | Extended live demo using a sentiment agent: user message tricks V1 into ignoring its task; used as the motivating scenario for versioning and qualitative eval. |
| Extra: Code versioning analogy (Git) | covered | Compared prompt versioning to GitHub/code versioning for audit trails and team collaboration. |
| Extra: Jitter and synchronized retries | covered | Explained adding jitter to backoff to avoid many clients retrying at the same instant (thundering herd); distinguished from Python async/sync terminology. |
| Extra: Prompt composition beyond system message | covered | Clarified a full prompt includes system message plus config parameters (model, temperature, max tokens), not text alone. |
| Extra: Tenacity as community standard | covered | Positioned Tenacity as the preferred library over manual `while`/`time.sleep` retry loops; noted it reruns the function after delay without creating threads by default. |
