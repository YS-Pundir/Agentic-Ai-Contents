| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Explain briefly why LLMs alone are not enough for organization-specific or up-to-date questions (knowledge cutoff, hallucination risk) | covered | Explained knowledge cutoff using current-election examples, showed that LLMs may answer from old/general knowledge, and discussed that private/company or resume-specific information requires supplied context. Hallucination risk was also connected later to grounding. |
| Define Retrieval-Augmented Generation (RAG) as giving the model a searchable external “library” instead of relying only on training memory | covered | Defined RAG as searching an external knowledge base/library, retrieving relevant context, adding it to the prompt, and letting the LLM answer using that material. |
| Walk through the five-step RAG flow once at a conceptual level: ingest → prepare → retrieve → augment prompt → generate | covered | Covered the five broad steps explicitly and explained ingest/data loading, prepare via chunking and embeddings, retrieval from vector store, prompt augmentation with retrieved context, and answer generation. |
| Explain retriever vs generator and grounding in plain language (answer from supplied context when the library has the fact) | covered | Explained retriever as finding relevant information from knowledge sources, generator as writing the answer, and grounding as answering only from retrieved notes/context and saying “I don’t know” when the fact is absent. |
| Compare one question answered without external context vs with context pasted from a sample document (manual demo — no full RAG app in this session) | covered | Demonstrated manual context use with examples such as resume/company/Tesla document context, contrasting a plain question without context against a question plus pasted/retrieved context. Confirmed this was a manual RAG-style demo, not a full RAG app. |
| Demonstrate how input text converts to embeddings using Ollama (extend the embeddings preview from the previous session) | covered | Demonstrated text-to-embedding generation with Sentence Transformers/Hugging Face and discussed Ollama embedding models such as nomic-embed-text as another way to do the same embedding generation. |
| Explain that embeddings will be stored in a vector database and, in upcoming sessions, retrieved and fed to the LLM to complete the RAG loop | covered | Explained saving chunks and their embeddings in a vector database/vector store, retrieving similar chunks through vector search/top-K, adding retrieved context to the prompt, and noted chunking, vector search, and the complete RAG pipeline would continue in upcoming sessions. |
| Extra concept: context window limitations | covered | Spent additional time explaining that large documents can exceed the context window and that RAG retrieves only relevant portions instead of sending the entire document. |
| Extra concept: privacy and cost benefits of RAG | covered | Discussed that RAG can avoid sending all organization data, reduce token usage, maintain privacy, and keep the process more economical. |
| Extra concept: chunking and top-K retrieval preview | covered | Went beyond the basic flow by explaining chunking examples, query embeddings, closest matching chunks, and top-K retrieval at a conceptual level, while deferring detailed tuning to later sessions. |
| Extra concept: RAG vs fine-tuning | covered | Discussed fine-tuning at a high level, including when it differs from RAG, cost/security concerns, and when RAG is better for external knowledge retrieval. |
