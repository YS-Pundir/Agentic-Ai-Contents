| topic / sub topic | status | remarks |
| --- | --- | --- |
| Understand that LLMs can run locally on your own machine, not only through paid cloud services | covered | Cloud vs local LLM analogy (cloud kitchen vs cook at home); cost (free local inference vs pay-per-token cloud); hardware constraints vs cloud GPU clusters; live demos planned and delivered in Ollama segment. |
| Install Ollama on your operating system and verify that the setup works | covered | Hands-on download from ollama.com (Windows EXE walkthrough; Mac noted); `ollama --version` in PowerShell/terminal/command prompt to validate install; GUI settings and models folder location shown. |
| Pull a light local model (for example 0.5B or 1B parameter models) and run a simple prompt from the CLI | covered | `ollama pull` for Qwen 2.5 0.5B (~397 MB); optional Llama ~1B (~1.3 GB); `ollama list`; `ollama run` with interactive prompt (e.g. photosynthesis in two sentences); `ollama pull` / `ollama run` recap. |
| Explain why learners should avoid pulling heavy models on regular laptops | covered | Model size examples (300 MB vs 47 GB); RAM/Task Manager demo; warning that large pulls can crash systems; instructor-only demo of heavier DeepSeek 14B pull/run (students told not to repeat). |
| Describe the trade-offs of very small local models, including limited training data and a higher chance of hallucination | covered | Ultra-light vs medium vs heavy model framing; limited multitask reasoning; high hallucination risk on smallest models; more parameters ≈ more knowledge but more compute; accuracy vs speed trade-off. |
| Write a short Python script that calls a local model through the Ollama API and prints the response | partially covered | End-of-session walkthrough of notebook-style snippet (`ollama.chat`, model name, system/user messages) and analogy to Groq client calls; not executed live in class—primary live Python was Groq in Colab (`client.chat.completions.create`). Students directed to follow uploaded class notebook. |
| Create an Ollama account and generate a free-tier Ollama API key | not covered | Ollama account mentioned in app settings and sign-in noted for some cloud models on ollama.com; no live Ollama API key creation. Live API key exercise was Groq (`console.groq.com`), not Ollama. |
| Update the same Python script to call Ollama Cloud by changing the API key and host settings | partially covered | Conceptual only: host points to ollama.com vs local; same code pattern as cloud providers; no live demo with Ollama Cloud API key or executed cloud call. |
| Compare local light-model output with Ollama Cloud output on the same prompt | not covered | Compared small vs larger local models (speed/quality) and Groq/OpenAI model outputs on same prompt; no side-by-side local Ollama vs Ollama Cloud on identical prompt. |
| List the main things Ollama can do, including chat, text generation, and embeddings | partially covered | Chat/interactive CLI via `ollama run` covered; modalities and “Ollama can do a lot of things” discussed; embeddings and text-generation-as-distinct-capability not named or demo’d in transcript. |
| Identify the main model types available through Ollama, including text, vision, audio, and video | covered | Modality overview on ollama.com/library (text, image, speech, video); vision models search; multimodal models tied to Gemini-style inputs. |
| Finish the session with one working Python script that can call either local Ollama or Ollama Cloud | partially covered | Groq notebook cells executed live with API key in secrets; Ollama programmatic path explained but not run end-to-end; unified local/cloud Ollama capstone not demonstrated live. |
| **[Extra]** Groq cloud API + Colab/Jupyter setup | covered | `console.groq.com` API key creation; secrets in notebook; live `client.chat.completions.create` execution; free-tier cloud inference vs local Ollama heat/slowness contrast. |
| **[Extra]** OpenAI and Anthropic API patterns | covered | Same client/message pattern across providers; platform URLs and different API keys noted; model swap in code without structural change. |
| **[Extra]** Same-prompt output comparison (cloud models) | covered | Same prompt across models (e.g. Groq vs OpenAI); JSON formatting differences; pricing differences mentioned. |
| **[Extra]** Chinese vs US-trained model bias | covered | DeepSeek/Qwen context; propaganda/training-data angle; live contrast prompts (e.g. Taiwan, Palestine, Iran) vs ChatGPT-style answers. |
| **[Extra]** Enterprise on-prem / local LLM use cases | covered | Confidential data (e.g. pharma/patient data); why orgs invest in on-premise local servers instead of public cloud APIs. |
| **[Extra]** Multimodal Gemini demo (Google Photos) | covered | Natural-language photo search (sunrise/sunset from flights); multimodality beyond text; deferred fuller RAG to next session. |
| **[Extra]** Tokens and local compute feel | covered | Token generation speed on small vs larger local models; CPU/RAM usage during heavier local run; why cloud feels faster on weak hardware. |
| **[Extra]** Hugging Face and Groq as cloud platforms | covered | Mentioned early alongside cloud LLM options for quick POCs when local hardware is insufficient. |
| **[Extra]** Ollama GUI vs CLI | covered | Desktop app (new chat, model browse, settings); CLI emphasized as primary workflow; cloud-tagged models on ollama.com vs downloadable local models. |
| **[Extra]** Prompting recap tie-in | covered | System/user messages, few-shot/zero-shot framing carried from prior session into API calls; not new LO content but reinforced during Groq segment. |
