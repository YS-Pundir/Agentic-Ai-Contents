| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Relate tokens to billing; latency; and prompt length decisions for designers. | covered | Taught tokens as LLM's smallest processing unit (not equal to words); API billing on input/output token count; ~4 characters per token rule; encoding schemes per model; higher tokens increase latency and generation time; longer prompts require trimming instructions; PDF uploads billed/chunked by tokens not page count; backpacking/travel analogy for lean prompts; live Google Colab demo with `tiktoken` showing word count (36) vs token count (64) on same text. |
| Explain how context limits constrain RAG and memory design choice. | covered | Context window framed as fixed tiffin box/shared desk budget including system instructions, RAG chunks, chat history, tool results, and output; truncation when limit exceeded (drop old content or reject input); RAG top-K tradeoff (relevance vs coverage vs cost); smaller chunks and incremental top-K tuning; Colab simulation packing chunks until max tokens; short-term memory = in-window conversation vs long-term via JSON/persistent storage and summaries; loop control paired with context limit in production agents; windowed-history demo showing older messages dropped when max message count exceeded. |
| Configure temperature or seed (where available) to trade creativity vs consistency in agent responses. | covered | Determinism vs non-determinism explained; temperature 0 ≈ deterministic (good for RAG/policy); 0.2–0.5 slight variation; 0.7–1 higher variance for brainstorming; seed for reproducible testing/debugging when available; motorcycle speed analogy for consistency vs creativity tradeoff; chat activity on refund policy (temp 0), lookup/tool planning (~0.3), SMS generator (higher creativity); live Groq demos comparing temp 0, 0.8, 1, and 1.2 on same prompt showing stable vs creative/hallucinated outputs. |
| Predict user-visible effects when context is truncated or overloaded in multi-turn agent loops. | covered | Multi-turn agent loop defined as repeated model→tool→model cycles; each lap adds tokens (heavier backpack); user-visible effects include forgotten prior context, renegotiation/confusion (auto-rickshaw analogy), contradictory answers, agent stops, hallucinations, and back-to-square-one in flight-booking example; token risk varies by stage (plan moderate, search high, tool execution medium, final answer highest); practical playbook—summarize tool observations >500 tokens, summarize/persist memory after ~20 turns, keep temperature low in loops, avoid too many steps; truncation causes reliability and UX drop. |
| Extra: Tokens vs words and tokenization fundamentals | covered | Spoken language uses words; LLMs use tokens (whole word, subword, punctuation, URLs, newlines); tokens carry no inherent meaning unlike words; special characters depend on encoding. |
| Extra: Context window composition for designers | covered | Breakdown of what fills the window (system prompt, user query, retrieved context blocks, chat history, tool observations, model reply); designer actions—declutter, avoid large PDF dumps, reserve 10–15% safety buffer in token budget. |
| Extra: LLM is not persistent storage | covered | Clarified LLM is a model not cloud storage; chat history persisted on disk/database; context window is ephemeral working memory. |
| Extra: RAG chunk-budget Colab exercise | covered | Hands-on simulation counting tokens per retrieved chunk, tracking usage against max context, and observing prompt composition when limit is lowered to 100 tokens. |
| Extra: Designer playbook and token budgeting heuristics | covered | Check whether each prompt includes RAG, history, and tools; plan per-model token budgets; per-message billing awareness; suggested rough allocations (e.g. system instructions, ~200 for chat history); test with fixed prompts/seeds and expected strings. |
| Extra: System prompt and grounding | covered | Q&A emphasized never omitting system prompts when trimming; grounding/RAG recommended for customer-facing apps to reduce hallucination. |
