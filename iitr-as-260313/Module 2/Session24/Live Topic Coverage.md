| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| This session: implementation-leaning mixture — extend Session 23 fetch-and-parse skills into agent tool use; wire the ShopKart assistant so the model can choose between policy retrieval (RAG) and a live weather API tool, then synthesise a customer reply with Groq; close Module 2 by tracing one full agentic action from user query to tool result to final answer | covered | Recap of Session 23 API/fetch work at start; full `tool integration` code walkthrough with ShopKart policy + weather tools on Groq; live demo of two customer queries including express-delivery + weather combo. End-of-session recap lists Module 2 themes (embedding, retrieval, generation, RAG, tools, external integrations) but no dedicated capstone segment explicitly labelled as closing Module 2. |
| Understand Function Calling — How Agents Choose Tools — Learners explain that an agent does not hard-code every branch; the model reads tool descriptions and selects a function (e.g. search policy documents vs fetch live weather) based on the user query; contrast this with the fixed GET-then-print flow from Session 23 | covered | Model selects tools from user intent (policy vs live weather, invoice → email tool, refund → payment tool); `tool_choice: auto` vs fixed tool choice explained. Session 23 contrast via last-class API GET flow vs LLM-driven tool selection in English/JSON; API-code integration vs tool-calling orchestration discussed. Explicit “GET-then-print” wording not used, but fixed manual API flow vs agent-chosen tools is clear. |
| Register Python Functions and API Wrappers as Agent Tools — Learners define callable tools with clear names and input schemas (city name, coordinates, or policy query); wrap the Session 23 weather GET helper and a RAG retrieval helper as separate tools the runtime can invoke and return structured JSON from | covered | Two tools registered: `search_shopcart_policy` (name, description, parameters) and `get_city_weather` (GET to Open-Meteo, lat/long, JSON return). Tool schema (name, description, parameters) walked through in code and PPT. Policy side uses in-memory keyword-matched policy snippets rather than Session RAG/ChromaDB retrieval — functionally a simplified policy-retrieval wrapper, not full vector RAG. |
| Run the Tool Execution Loop — Call, Return, Continue — Learners execute the model → tool call → Python function → JSON result → model reply cycle; handle tool errors (non-200 API, empty retrieval) with safe messages instead of invented facts | partially covered | Full loop demonstrated: system prompt + user message → Groq proposes tool calls → local `for` loop executes Python functions → tool results appended to messages → final Groq reply. Weather helper checks HTTP 200 before processing; grounding prompt says “do not invent any particular number.” Non-200 failure paths and empty-retrieval safe-message handling not live-demoed or coded explicitly; Mentimeter Q2 reinforces “propose tool” as first loop step. |
| Combine RAG and Live API Data in One ShopKart Reply — Learners demonstrate a customer question that needs both policy context and live weather (e.g. express delivery during rain in a metro city): retrieve grounded policy excerpts, call the weather tool, route extracted JSON fields into a single Groq prompt, and produce a final answer that cites policy rules and live conditions without hallucinating either source | covered | Demo query “express delivery to Delhi metro tomorrow — policy timing + weather/courier handoff” run live: policy tool returns 1–2 day metro express rule; weather tool returns ~33°C and weather code; final answer combines both. Instructor also noted model correctly refuses to invent how weather affects delivery when policy has no such rule — good grounding example. |
| Extra concept taught: MCP servers and third-party tool ecosystems | covered | Extended discussion — Alpha Vantage, IRCTC, Air India, Zomato, Razorpay, Slack/WhatsApp/email examples; MCP as English-speaking intermediary vs hand-written APIs; verified MCP sources; future class (~36th/40th) preview. |
| Extra concept taught: API integration vs tool calling (English/JSON vs Python code) | covered | Contrasted writing GET/request code (Session 23 style) with LLM sending structured English/JSON tool requests; third-party vendors provide tool implementations. |
| Extra concept taught: Groq runs in cloud — local tool functions executed on learner machine | covered | Explained Groq cannot run undeployed local functions; runtime parses Groq tool-call JSON and invokes local Python handlers, then sends results back. |
| Extra concept taught: Open-Meteo weather API | covered | Live API used in demo; instructor compared returned temperature with open-meteo.com. |
| Extra concept taught: n8n “mother MCP server” aggregating many MCP servers | covered | Q&A segment on connecting one hub MCP to many downstream integrations. |
| Extra concept taught: Orchestration diagram — user query → tool pick → JSON call → tool response → final LLM reply | covered | Whiteboard/diagram walkthrough for ShopKart refund/invoice scenarios plus flight-search JSON schema example. |
| Extra concept taught: Mentimeter quiz on tool calling | covered | Eight MCQs — tool selection, execution loop order, schema purpose, `tool_choice: auto`, `tool_calls` structure, `json.loads`. |
