| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| This session: hands-on implementation — scale the Session 20 minimal RAG to real policy files (PDF/txt) for the same e-commerce company; policy update workflows and deep chunk-tuning loops deferred to Session 22 | covered | Full RAG pipeline built on pre-written `code.py` (~400 lines): load policy files from `policy_documents` folder (PDF/txt via PyPDF), chunk, embed (BGE), store in ChromaDB, retrieve, and generate with Groq — scaling Session 20’s pre-chunked demo to real files. Instructor noted chunking strategy can be changed later (basic 100-word fixed chunking used now); policy update workflows and deep chunk-tuning loops were not covered (implicitly deferred). Session 22 not named explicitly in the live session. |
| Explain Why Raw Documents Must Be Prepared Before Retrieval — Learners articulate why full policy files cannot be searched effectively as-is — and what ingestion (loading and preparing text) enables in a RAG pipeline | covered | Instructor explained that RAG cannot work without raw data, and walked through ingestion as load → clean/normalize → chunk → embed → store. Stated explicitly: “without data how can you build rag.” Rationale for why whole files are not searched directly is conveyed through the required pipeline stages rather than a dedicated “as-is search fails” explanation; no structured learner articulation exercise. |
| Apply Chunking With Awareness of Retrieval Impact — Learners split policy documents into searchable units and explain how chunk boundaries and size affect which information surfaces for a given query | covered | Hands-on 100-word fixed chunking implemented and demonstrated; instructor contrasted with Session 20 (data already in chunk format). Mentioned alternative strategies (recursive, paragraph-based) for later change. Retrieval impact shown live via top-k (default 3), distance scores, and rank ordering for demo queries. Chunk-boundary overlap discussed in end-of-session quiz (preserve context across boundaries); no live chunk-size tuning comparison. |
| Build a Searchable Index Across Multiple Policy Sources — Learners transform chunked policies into a retrievable knowledge index (embedded vectors with meaningful metadata) and verify retrieval across different policy types | covered | Four policy documents loaded, chunked, embedded, and indexed in ChromaDB; instructor described extracting row id, document text, and metadata during indexing. Live run confirmed four documents indexed and knowledge base complete. Retrieval verified across return, shipping, warranty/repair, and refund query types with distance-based ranking. Metadata/category-from-filename reinforced in quiz, lightly referenced during code walkthrough. |
| Operate a Multi-Document RAG Assistant End-to-End — Learners demonstrate the complete assistant answering questions that span returns, shipping, warranty, and refund policies — using the same e-commerce scenario from Sessions 19–20 | covered | End-to-end demo with four representative customer queries: (1) unopened phone return window, (2) express shipping delivery timeline, (3) wireless earphones warranty/repair after 10 months, (4) defective-kit UPI refund timeline. Full retrieve → grounded-generate loop run with ShopCart customer-support system prompt (carried from Session 20). |
| Extra concept taught: PyPDF (`pypdf`) as document loader for PDF policy files | covered | `pip install pypdf` / `pip3 install pypdf` demonstrated; document loader role explained in end-of-session quiz. |
| Extra concept taught: Required dependencies recap — chroma-db, sentence-transformers, groq, python-dotenv, pypdf | covered | Instructor listed all packages needed to run the pipeline; installation troubleshooting covered during live coding. |
| Extra concept taught: BGE embedding model and ChromaDB collection setup (continuation from prior sessions) | covered | BGE-small embedding model creation and Chroma collection for storing embeddings revisited as pipeline setup steps. |
| Extra concept taught: Top-k retrieval, distance scores, and multi-rank context sent to LLM | covered | Top-k=3 demonstrated; distances printed (e.g., 0.48 rank 1, 0.65 rank 2); instructor explained sending top-3 ranks (not only rank 1) to Groq for combined grounded answers. |
| Extra concept taught: Git clone, `policy_documents` folder setup, and `.env` / Groq API key configuration | covered | Significant session time on repository clone, correct folder naming (`policy_documents`), pushing/pulling sample data, and Groq API key in `.env` (recap from Session 20). |
| Extra concept taught: Three-class progression arc — vector search (Session 18) → minimal RAG (Session 19–20) → full RAG pipeline with real files (today) | covered | Instructor summarized continuity across last three classes and pointed learners to watch prior recordings in order. |
