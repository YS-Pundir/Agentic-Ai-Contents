| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| This session: hands-on Chroma implementation only (assume vector database concepts, indexing, and ANN from the previous session — do not re-lecture theory) | covered | The session was primarily hands-on around ChromaDB setup and implementation. There was a short recap of RAG, embeddings, SQL-style storage, dimensions, and distance, but indexing/ANN theory was not re-lectured in depth. |
| Bridge from Previous Session — Recall the embed → store → query → top-k pipeline; introduce Chroma as today's implementation tool | covered | The instructor connected the class to the previous RAG/vector-search discussion, explained the flow of converting data into embeddings, storing it, querying it, and returning top ranked matches, then introduced ChromaDB/Chroma as the tool for the session. |
| Chroma Terminology and Setup — Map Chroma concepts (client, collection, id, document, metadata, embedding) to familiar **SQL** ideas (tables/rows); install and configure PersistentClient | covered | ChromaDB installation with `pip`/`pip3`, Python/VS Code setup, `PersistentClient`, local Chroma store folder, collection creation, ids, documents, metadata, and embeddings were explained. SQL/table/row comparison was covered through `get_or_create_collection` and database/table analogies. |
| Add Data to a Chroma Collection — Prepare sample FAQ data, generate embeddings with a pretrained model, upsert vectors with ids, documents, and metadata; verify with count, peek, and get | covered | Sample e-commerce/FAQ records were prepared, documents/ids/metadata were extracted, a SentenceTransformer model generated embeddings, and `upsert` inserted ids, documents, metadata, and embeddings into Chroma. Verification was shown using `count`, `peek`, and `get`. |
| Retrieve Data from a Chroma Collection — Embed a user query with the same model, run top-k similarity search with query(), interpret returned chunks, ranks, and distance scores; discuss one wrong-result example at interpretation level only | covered | User queries such as returning shoes and changing a login password were embedded with the same model and searched using `query()`. Top-k results, ranks, metadata/documents, distance values, and the idea that lower distance means closer meaning were discussed. A password query example showed that only the first result was useful while another returned result was less relevant due to larger distance. |
| Bridge to Next Session — Brief note that the RAG track will reuse this Chroma collection as the retriever layer (metadata filtering, collection updates, and retrieval tuning deferred to later RAG sessions) | covered | The instructor briefly noted that the same code would be reused later to create a RAG project and that deeper Chroma/internal database details would be discussed later. Metadata was introduced as useful for filtering, but collection updates and retrieval tuning were not explicitly discussed. |
| Extra concept taught: Python, terminal, and package installation troubleshooting | covered | Significant time was spent on checking Python versions, choosing `pip` vs `pip3`, installing ChromaDB/SentenceTransformer-related dependencies, and resolving setup issues for students. |
| Extra concept taught: SQLite Viewer / inspecting Chroma storage files | covered | The session included installing a SQLite Viewer extension, opening the Chroma SQLite file, and explaining that Chroma stores collection and embedding-related data in local database files. |
| Extra concept taught: Embedding dimensions and model choice | covered | The instructor explained that the chosen SentenceTransformer model outputs 384-dimensional embeddings, compared this with larger models such as BGE Large, and noted that different models can have different dimensions and accuracy. |
| Extra concept taught: Git/GitHub workflow for sharing session code | covered | The instructor demonstrated `git status`, `git add`, commit, push, and `.gitignore` usage while sharing updated session code, though this was outside the vector-search learning objective. |
| Extra concept taught: End-of-session quiz on Chroma commands | covered | The quiz reinforced `count`, `query`, metadata, `peek`, and lower distance meaning nearer semantic similarity. |
