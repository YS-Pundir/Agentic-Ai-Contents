| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| This session: hands-on implementation — build a minimal working RAG loop for the e-commerce customer support assistant; reuse the Session 18 Chroma vector search setup as the retriever layer (top-k tuning and metadata filtering deferred to Session 22) | covered | Full ShopCart RAG implementation built (`rag.py`): ingestion (BGE embeddings + ChromaDB), retrieval (top-k vector search reusing Session 18 Chroma setup), and generation (Groq/Llama). Multiple representative queries demoed. Top-k tuning discussed (top 2/3/5); metadata filtering explicitly deferred to next class. |
| Frame Customer Support as a RAG Problem — Learners articulate the assistant's purpose, users, and knowledge boundaries — and identify which policy documents (returns, shipping, warranty, refund) constitute the knowledge base | covered | Instructor framed ShopCart e-commerce support as a RAG use case (customer, support team, business scenarios). Knowledge base / knowledge boundary explained. Four policy chunks explicitly identified: return, shipping, warranty, and refund. Reinforced in end-of-session quiz (knowledge boundary = topics the bot is allowed to use). Framing was instructor-led rather than a structured learner articulation exercise. |
| Demonstrate Semantic Retrieval of Relevant Policy Content — Learners show that a customer query retrieves the most relevant policy excerpt from the knowledge base — and can judge whether retrieval matched the question's intent | covered | Retrieval function built and demonstrated with live output: user query embedded, top-k matches retrieved from ChromaDB with distances printed. Examples included return window, phone-case return, express shipping, warranty/repair, and UPI refund queries. Instructor discussed judging match quality via distances and whether retrieved chunks align with query intent. |
| Produce Answers Grounded in Retrieved Evidence — Learners generate responses that reflect retrieved policy text rather than the model's general knowledge — and can explain how context shapes the output | covered | Generation step implemented with Groq client: retrieved chunks passed into a system prompt as context block, customer-support persona and rules applied (prompt-engineering techniques referenced). Multiple grounded answers demonstrated (e.g., 7-day return window, delivery timelines, warranty/refund responses). Instructor walked through how retrieval context flows into the prompt and shapes the final answer. |
| Validate a Minimal RAG Pipeline Against an LLM-Only Baseline — Learners run the full retrieve → generate loop on representative queries and justify why grounded answers are more accurate than standalone LLM responses, connecting back to Session 19 | not covered | Full RAG loop was run on multiple queries, but no live side-by-side LLM-only (no-retrieval) baseline comparison was executed. Instructor briefly mentioned calling the LLM without retrieved context but did not run or justify a comparison. Session 19 ungrounded-vs-grounded contrast referenced only via end-of-session quiz question, not as an in-session validation exercise. |
| Extra concept taught: RAG architecture components — ingestion, retrieval, and generation pipeline with end-to-end diagram walkthrough | covered | Significant time spent breaking down the three RAG components, raw data → chunk → embed → ChromaDB flow, and the full retrieve → augment → generate loop mapped to code. |
| Extra concept taught: Groq API integration and `.env` file for secure API key management | covered | Dedicated segment on creating `.env` / `.env.example`, `GROQ_API_KEY`, `python-dotenv` install, and verifying API calls via Groq dashboard. |
| Extra concept taught: BGE embedding model fundamentals and embedding dimensionality (384-dim vectors) | covered | BGE model from Beijing AI Institute explained; embedding defined as numeric sequences; four 384-dimensional embeddings demonstrated for four policy chunks. |
| Extra concept taught: Cosine similarity as ChromaDB's internal retrieval mechanism (preview for next session) | covered | Brief discussion that ChromaDB uses cosine similarity for nearest-neighbor search; noted it can be overridden/customized in future sessions. |
| Extra concept taught: Embedding variability / non-determinism across runs | covered | Instructor explained that embedding values and distances may differ across runs or users because the model is predictive rather than producing fixed deterministic numbers. |
| Extra concept taught: Real-life customized RAG project references and demo links | covered | Instructor shared links to larger real-world RAG implementations (customized RAG demos) as extended learning references beyond the minimal lab build. |
