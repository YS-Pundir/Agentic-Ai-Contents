| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| This session: analyze and improve the Session 21 e-commerce RAG assistant — test with realistic customer queries, diagnose failures, apply targeted fixes, and establish an iterative evaluation mindset | covered | Hands-on extension of Session 21 codebase (`improving rag` on GitHub); seven realistic e-commerce customer queries added and run end-to-end; baseline (Session 21) vs improved outputs compared live; targeted fixes applied (strict prompt, category filter, top-k=5). Iterative mindset reinforced mainly via compare-and-improve demo flow and end quiz rather than a standalone production-cycle lecture. |
| Diagnose Failure Modes in RAG Outputs — Learners classify failures as retrieval problems (wrong or missing chunks), generation problems (answer does not match evidence), or hallucinations (answer not supported by retrieved text) | covered | Live diagnosis demonstrated on sample queries without formal taxonomy labels: hallucination (COD kettle refund inventing UPI/payment details not in policy), retrieval limitation (metadata filter returning only one rank/category when answer spans multiple policies), generation misinterpretation (inferring prepaid from COD context). Formal retrieval vs generation vs hallucination definitions reinforced in Mentimeter quiz (Q2–Q4); no structured learner classification exercise during the live walkthrough. |
| Evaluate Retrieval and Generation as Separate Stages — Learners test the assistant with realistic customer queries and independently judge (a) whether the right policy was retrieved and (b) whether the final answer faithfully reflects it | covered | Instructor compared baseline vs improved sections for each of seven queries — inspecting top-k ranks, retrieved chunks, and final answers separately (e.g., UPI prepaid query, defective kettle COD refund, open laptop charger return). Discussed when retrieval was insufficient vs when the LLM misread correct context. Quiz Q5–Q6 reinforced inspecting rank-1 chunk first and evaluating stages independently. |
| Apply Targeted Improvements Based on Diagnosis — Learners improve system quality through at least two levers — e.g. top-k adjustment, chunk refinement, metadata filtering by policy type, or stricter grounding prompts — and observe the effect on sample queries | covered | Three levers implemented and demonstrated: (1) top-k increased from 3 to 5, (2) stricter grounding/strict prompt with refusal rules, (3) metadata/category filter via `guess_category` + ChromaDB `where` filter. Effects observed by comparing baseline and improved outputs on the same queries. Chunk refinement discussed in Q&A (mutual-fund per-document chunking example) but not applied as a fix in this session's code. |
| Adopt an Iterative Evaluation Mindset for Production RAG — Learners explain why building RAG is only the first step: ongoing testing, user feedback, and refinement are required for reliable production systems — and outline a simple repeat cycle (test → diagnose → improve → re-test) | covered | Session itself models iteration on Session 21 build. Mentimeter quiz Q1 and Q8 explicitly cover diagnose-first and test → diagnose → improve → retest workflow. `guess_category` described as hit-and-trial refinement. No dedicated segment on ongoing production testing or user-feedback loops beyond quiz answers. |
| Extra concept taught: `guess_category` keyword-based routing (return/shipping/warranty/refund) | covered | If-else keyword heuristic maps user queries to policy categories before retrieval; instructor noted it is improvable and not always accurate, with fallback to full-index search when no category matches. |
| Extra concept taught: ChromaDB metadata filtering (`where` on category) | covered | Live demo showed retrieval restricted to a single category embedding set; metadata (file name, category, chunk index) printed and stored during indexing. |
| Extra concept taught: Strict prompt design and prompt comparison | covered | Compared Session 21 vs Session 22 prompts side-by-side (online diff); added harder refusal rules (“do not invent rules”, exact refusal sentence); noted prompts can be generated via ChatGPT/Gemini/Claude. |
| Extra concept taught: Single-category filter limitation and multi-category retrieval as future improvement | covered | Instructor diagnosed that advanced version fails when an answer requires chunks from multiple policy categories simultaneously; discussed extending `guess_category` to support multiple categories. |
| Extra concept taught: Groq API free-tier rate limits affecting live demos | covered | Significant time on token/request limits (30 req/min, daily cap), 98–99% usage consumed during prep, and impact on answer generation during live run. |
| Extra concept taught: `output.txt` for preserving and comparing demo results | covered | Pre-saved output file used to walk through baseline vs improved sections without re-running all queries live. |
| Extra concept taught: Whiteboard/diagram — subdividing large RAG index by metadata subcategories | covered | Theoretical diagram redrawn: route user query to category first, then search embeddings within that subset rather than the full vector store. |
| Extra concept taught: Milestone RAG chatbot project and advanced RAG follow-up video | covered | Brief mention of upcoming milestone (complicated RAG chatbot built with vibe coding) and promise to share an advanced RAG video after class. |
