| Topic / detailed sub-topic | Status | Remarks |
| --- | --- | --- |
| Query Relational Data with JOINs — Introduce INNER JOIN using a two-table example (users and orders); write a hands-on query that combines both tables to retrieve meaningful results (e.g., which user placed which order); understand what a JOIN does conceptually before writing it — limited to two-table joins only | Covered | ~40 min spillover: cross join (Cartesian product) on `customers` × `orders`, then conditional join on `customer_id` with meaningful rows (John/keyboard, etc.); aliases (`c`, `o`); ORDER BY. Join-with-`ON` behavior matches INNER JOIN though the term **INNER JOIN** was not used live (only in closing quiz). Other join types deferred (“go back and see other joints part”). **Extra:** table/column alias renaming; whiteboard/dry-run tool demo. |
| Perform CRUD on Related Data — Insert, update, and delete records across related tables while maintaining referential integrity; understand cascading behavior with simple examples | Not covered | `UPDATE` mentioned only in passing (aliases vs changing original tables). No coordinated INSERT/UPDATE/DELETE across related tables, referential integrity, or cascading in live teaching. |
| Bridge from Session 14 Preview — Short recap of embeddings, semantic similarity, and where they fit in RAG; pivot to today's focus | Partially covered | After joins, brief pivot (“before actually starting in the rag part”) into vectors/embeddings and RAG dependency; no structured Session 14 preview recap of embeddings/semantic similarity as a dedicated segment. |
| Revisit Why Meaning-Based Search Matters — Why keyword and exact-match retrieval fail in real AI use cases and why systems must interpret intent and meaning | Covered | AI/computers do not natively understand meaning; keyword match fails on remote work vs work-from-home; meaning-based retrieval needed for real Q&A (quiz reinforced “meaning and intent”). |
| Define Vectors and Embeddings — Vectors as ordered lists of numbers; embeddings as semantic vector representations of text; granularity at word, sentence, and document level | Covered | Vectors as lists of numbers; embeddings as numbers representing meaning; word-level (dog/Labrador) and sentence-level examples; paragraphs/PDFs/URLs/chunks as embeddable content. Document-level named less explicitly than word/sentence/paragraph chunk level. |
| How Text Becomes Embeddings — High-level text-to-vector pipeline; connect to tokenization from prior sessions; embedding models at a high level (same model for documents and queries) | Covered | Raw text → tokenization/chunking → embedding model (BGE, OpenAI, Google, etc.) → sequence of numbers; same model for stored data and user query emphasized; live chunk → embedding walkthrough. **Extra:** chunking strategies (fixed-size, semantic, paragraph-based, recursive/nested) and data scraping/cleaning before embed. |
| Semantic Similarity and Vector Space — Similar meanings as closer vectors; vector space intuition; conceptual similarity measurement (distance/angle) without mathematical detail | Covered | Closer vectors = similar meaning (dog/German Shepherd, food vs banking clusters); balloon/vector-space diagram; proximity in space. **Extra:** cosine similarity named and explained at a high level (beyond “no math” wording in metadata). |
| Differentiate Keyword Search vs Semantic Search — When each approach wins; compare exact-match and meaning-based retrieval | Covered | SQL `LIKE`/wildcards vs embedding + cosine match; Control+F example; keyword fast but no meaning; semantic for paraphrases; production can combine both; structured comparison after break. |
| Understand Semantic Search Workflow — Store content, embed documents, embed query, compare vectors, return closest matches; defer indexing, ANN, and vector DB implementation to Session 17 | Covered | End-to-end steps: collect/scrape data → clean → chunk → embed chunks → store → embed query → compare (cosine) → top matches → LLM answer; Swiggy/Zomato FAQ example; password-reset flow. Indexing/ANN/vector DB ops deferred to next session as planned. |
| Explore Applications and Embeddings in Agentic Systems — Semantic search, recommendations, conversational AI, RAG retrieve step, and agent memory or context retrieval by meaning | Partially covered | Semantic search, chatbots/support bots, naive RAG retrieve + generation, real-time company Q&A demo (HDFC fund data), long-term/semantic vs episodic memory, agentic/graph RAG teased. **Not covered:** recommendations as an application. |
| Bridge to Session 17 — Vector databases store and retrieve embeddings at scale; covered next session | Covered | Vector DB for storing embeddings and similarity search (vs SQL); millions of chunks, Pinecone, detailed vector DB + RAG in next class. |
