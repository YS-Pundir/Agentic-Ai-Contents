| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| This session: conceptual only (no Chroma setup or live coding lab — that is the next session) | covered | Session remained primarily conceptual. The instructor repeatedly stated that Chroma implementation, actual embedding model usage, and running the pipeline would happen in the next class. |
| Bridge from Previous Session (10–15 mins) — Recap embeddings, same embedding model for documents and queries, and semantic search workflow; confirm readiness before vector database concepts | covered | Recapped chunking documents, converting chunks to embeddings, embedding the user query, matching nearest vectors, sending retrieved context plus query to the LLM, and checked learner readiness before moving into vector databases. |
| Recall the Role of Embeddings in AI Systems — Short revisit only; how embeddings enable similarity-based retrieval (do not re-teach full embedding theory from the previous session) | covered | Revisited embeddings as lists/vectors of numbers representing meaning, explained similar words or sentences staying closer in vector space, and connected embeddings to retrieval. This was more than a very short revisit but still focused on retrieval context. |
| Identify Limitations of Traditional Databases for Vector Data — Explain why relational databases are not suitable for efficient vector similarity search at scale; contrast exact-key SQL lookup vs nearest-meaning search | covered | SQL exact lookup and keyword matching were contrasted with semantic search using examples like names starting with `J`, `admin` vs `administration`, `work from home` vs `remote work`, revenue/order lookup vs MFA wiki search, and high-dimensional embedding comparison limitations. |
| Introduce Vector Databases — Define vector databases and their role in storing, indexing, and retrieving embeddings; mention example tools at a high level (e.g. Chroma, Pinecone, pgvector) without hands-on setup | covered | Defined vector databases as systems for storing embeddings, indexing them, and retrieving relevant vectors. Mentioned Chroma DB, Pinecone, pgvector, and also FAISS. Some tool/file viewing discussion happened, but no full setup or live lab was performed. |
| Understand Similarity-Based Retrieval — Explain how vector databases return results based on semantic closeness (nearest vectors) rather than exact text or key matches | covered | Explained nearest-neighbor retrieval, returning closest vectors by meaning, retrieving multiple relevant chunks, and passing context to an LLM. Examples included remote work, fees/installment queries, and product recommendations. |
| Develop Intuition for Similarity Search — Use real-world analogies to explain how similarity-driven retrieval works in production systems | covered | Used multiple analogies and examples, including coaching institute fee queries, Amazon product recommendations, Ola/Uber nearby cabs, post office code clustering, and vector-space grouping. |
| Understand Similarity Measurement (Conceptual) — Reinforce only (~5 mins); closeness via distance/angle (e.g. cosine similarity) without formulas — do not repeat the full lesson from two sessions ago | covered | Covered cosine similarity conceptually using angle-based intuition, 3D point diagrams, and contrast with simple distance/dot-product style comparison. This section took meaningful time and included small program-style examples, but remained conceptual. |
| Understand the Need for Scalable Search — Explain why brute-force comparison across all vectors is inefficient as data grows | covered | Covered brute-force vector search as comparing the query against every stored vector, linked it to Big O(n), explained that it becomes slow at large scale, and reinforced this in the quiz. |
| Introduce Vector Indexing — Explain how indexing structures organize vectors to enable faster search than full scans | covered | Explained indexing as grouping/clustering closer vectors so search can avoid scanning all vectors. Used cluster diagrams, post-office-code analogy, and discussed how vector databases arrange nearby embeddings. |
| Understand Approximate Nearest Neighbor (ANN) Search — Explain how systems balance speed and accuracy by retrieving near-optimal results at scale | covered | Explained ANN as approximate nearest neighbor search, using grouped search/hopping to find top results faster than brute force. Covered trade-off: faster search with possible small accuracy loss versus brute-force accuracy but slower performance. |
| Understand the Similarity Search Process — Conceptual end-to-end: query embedding → search indexed collection → return nearest vectors (diagram/story level; implementation is next session) | covered | Covered ingestion and runtime flow: get documents, chunk, embed, store in vector DB, embed query, search via ANN/semantic search, retrieve top-K chunks, pass context plus query to LLM, and generate an answer. |
| Differentiate Exact Match vs Similarity Search — Brief recap only (~5 mins); when keyword/SQL exact match wins vs semantic vector search — do not repeat the full comparison taught previously | covered | Revisited exact lookup vs meaning-based search with SQL and vector DB decision examples. This went beyond a brief recap, but the distinction was clearly covered. |
| Bridge to Next Session — Next session implements the pipeline in code (Chroma): embed, store, query, top-k, and a minimal end-to-end script | covered | Clearly stated that the next session would implement the theory with Chroma DB, Python, embeddings, storing, querying, and top-K retrieval. |
| Extra concept: Top-K retrieval | covered | Spent time explaining top-K/top 3/top 5/top 10 retrieval as returning the best matching chunks rather than a single result; also included in the quiz. |
| Extra concept: Big O notation, linear search, and binary search analogy | covered | Introduced time complexity and Big O notation, explained O(n) linear search, and used binary search/log-time intuition to motivate faster vector search approaches. |
| Extra concept: FAISS and HNSW/indexing algorithms | covered | FAISS was introduced as Facebook AI Similarity Search, and HNSW/partitioning algorithms were mentioned as internal indexing/search mechanisms, while detailed math was deferred. |
| Extra concept: Metadata storage with embeddings | covered | Mentioned that vector databases can store textual data and metadata along with embeddings, using stock-market data metadata as an example for filtering/context. |
| Extra concept: K-means/KNN discussion | covered | Brief Q&A touched on K-means clustering and K-nearest neighbors as related clustering/search ideas, but this was not developed deeply. |
