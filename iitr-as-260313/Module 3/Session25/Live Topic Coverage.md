| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Demonstrate successful Ollama installation and validation using a laptop-appropriate local model tier | covered | Install demonstrated via direct download, macOS Homebrew (`brew install ollama`), and terminal; validation with `ollama --version`; desktop app walkthrough; `ollama pull` for Qwen (small/fast laptop tier); `ollama run`, `ollama list`, `ollama rm`; live chat demo in terminal and GUI app; learners prompted to install and verify in parallel. |
| Implement programmatic chat completion against Ollama from Python | covered | `pip install ollama` shown; basic `code.py` written with model name, user question, and chat call; live run after pulling Qwen; model-not-found error demonstrated and resolved via `ollama pull`; second example with custom prompt (photosynthesis definition); `ollama.chat` with user role/messages explained. |
| Compare local versus Ollama Cloud behavior on an identical prompt using environment-driven configuration | covered | Dual-mode script on GitHub: `USE_CLOUD=0` runs local Qwen, `USE_CLOUD=1` runs Ollama Cloud (GPT-OSS); same train aptitude problem run both ways live; local model gives wrong answer (9 AM), cloud model gives correct answer (~7:34 PM); output prints mode/model/question/answer; brief cross-check with ChatGPT/Gemini on same question. |
| Apply secure credential handling so secrets stay out of version control while access remains reproducible for the cohort | partially covered | `.env` file created with `USE_CLOUD` and `OLLAMA_API_KEY`; `dotenv` import/load shown; required variable names shared in Zoom chat and end-of-session recap; program pushed to GitHub for cohort reuse. Explicit guidance on keeping secrets out of version control (e.g. `.gitignore`, not committing `.env`) not stated in the transcript. |
| Extra concept taught: Local LLMs — benefits, limitations, and when to use Ollama | covered | Free/no API cost, data privacy, no cloud data transfer; disadvantages of tiny models (weak reasoning, knowledge cutoff); Ollama positioned as learning/play tool, rarely used in production. |
| Extra concept taught: Ollama CLI commands (`pull`, `run`, `list`, `rm`, `/bye`) | covered | Full command walkthrough and post-break revision; Mentimeter quiz reinforces version, pull, and run commands. |
| Extra concept taught: Ollama desktop app vs terminal installation | covered | Both application and terminal installs shown; terminal preferred for Python integration; Windows and Mac install URLs shared. |
| Extra concept taught: Model size vs RAM / laptop constraints | covered | Small models (e.g. Qwen 0.5B) run on typical laptops; larger models need 40+ GB RAM or Ollama Cloud. |
| Extra concept taught: Ollama Cloud account and API key setup | covered | Sign-up at ollama.com (GitHub login); API key creation in Settings → Keys; cloud host `ollama.com`; client authentication for cloud calls. |
| Extra concept taught: Knowledge cutoff and local model training data (not laptop files) | covered | Q&A on whether local LLM reads laptop files; explained models use pretrained weights, not local file data. |
| Extra concept taught: Web search toggle in Ollama app | covered | Brief demo — enabling web search requires online sign-in / Ollama Cloud. |
| Extra concept taught: Mentimeter quiz (8 questions on Ollama) | covered | Covers Ollama purpose, version check, pull, run/chat, tiny-model benefits, Python `ollama` package, `chat` function, and `USE_CLOUD=1` meaning. |
