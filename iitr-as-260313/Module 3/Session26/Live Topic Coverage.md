| Topic / Sub-topic | Status | Remarks |
|---|---|---|
| Explain LangChain's definition and purpose | Covered | Explained as a framework providing pre-written code/components to integrate LLM with tools, memory, RAG; analogy with web frameworks (React/Vue/Next.js). |
| Justify framework use over ad hoc LLM API calls (composability & maintainability) | Covered | Showed how 20-100 lines of manual integration code reduce to a single function call; emphasized easy provider/model switching (Ollama local/cloud, Gemini to Groq). |
| Position LangChain on the agentic stack (between model providers and application logic) | Covered | Used a frontend - backend - API - AI agent diagram; placed LangChain inside the backend AI-agent layer handling tool calling, memory, retrieval, generation, LLM interaction. |
| Describe the Runnable-based composition model | Covered | Explained Runnable components take input and produce output (scraping, normalization, chunking examples); chains connect components and pass data via arrows. |
| Map LangChain's modular surface (chains, agents, tools, memory, retrievers) to a single-agent workflow | Partially Covered | Components named and described conceptually (tools, memory, retrieval, agents, chains), but only Prompt and LLM modules were demonstrated in depth. Agents, tools, and memory were explicitly deferred to upcoming classes. |
| Distinguish Core vs Community packages for typical project needs | Covered | Demonstrated `langchain.core` vs `langchain.community` (vector stores/DBs), plus provider packages `langchain_groq`, `langchain_ollama`, `langchain_openai`; community package not yet installed/used. |
| Author reusable prompts with PromptTemplate | Covered | Showed `PromptTemplate.from_template` with variables (topic, audience, tone, limit); contrasted hard-coded vs templated prompts and noted variables can come from user input. |
| Author reusable prompts with ChatPromptTemplate | Not Covered | Only briefly referenced "chat prompt from messages" in a tool-calling code snippet; ChatPromptTemplate was not actually taught or authored. |
| Compose a first chain using LCEL and the pipe operator | Covered | Explained the pipe `|` operator as an arrow connecting prompt to LLM to output parser; introduced LCEL (LangChain Expression Language) as declaring execution order. |
| Apply output parsers for predictable string handling | Covered | Introduced output parser (string output parser) from LangChain Core to convert raw LLM output into clean/predictable string format; mentioned JSON/list formats exist. |
| Instructor-led walkthrough of a minimal PromptTemplate to LLM chain end-to-end | Covered | Walked through 3 functions: raw Ollama, templated prompt + ChatOllama, and full LCEL chain (prompt - LLM - parser); ran and compared outputs (some delay due to laptop/setup issues). |
| Hard-coded vs templated prompt comparison (extra) | Covered (extra) | Spent additional time distinguishing fixed hard-coded prompts from variable-driven templated prompts; linked back to RTCF prompting technique. |
| When to use LangChain vs building from scratch (extra) | Covered (extra) | Extended discussion using tailor vs ready-made suit analogy; explained generic RAG/simple agents suit LangChain, while complex apps need custom chunking/embedding/retrieval. |
| Ollama setup & CLI usage (extra) | Covered (extra) | Significant time spent on `ollama run/pull/list/rm`, running local Qwen model, pip installing `langchain-core`, `langchain-ollama`; troubleshooting (incl. a learner's Chinese-language output). |
| N8N vs LangChain distinction (extra) | Covered (extra) | Clarified N8N is a no-code agent-building tool whereas LangChain is a code framework that minimizes integration code. |
| Real-world example: Swiggy-style chatbot (extra) | Covered (extra) | Showed a LangChain-based support chatbot project (refund status, wrong payment, issue identification) to illustrate that internal functions need not be memorized. |
